# Coding Agent 自进化能力升级方案（待实现）

> 用途：从"评测回归闭环"升级到"真正的自进化"，面向后续编码实现。
> 状态：**规划中，面试后实现**（面试前不改代码）。
> 基础：基于现有 `benchmarks/runner.py` 回归机制 + `_verifiers.py` 验证器 + 项目已有分层记忆/记忆召回。

---

## 0. 现状与差距（先说清楚）

### 当前已实现（评测回归闭环）
```
改代码 → 跑 runner → 每个任务驱动真实 Agent 执行 → 验证器打分
→ 失败任务写入 benchmarks/regression.yaml（进回归集）
→ 下次运行自动重新加载回归任务，重新跑一遍
```
- 任务 `description` → 作为 **user prompt** 直接进 Agent 上下文
- 失败案例**只进回归集重跑**，`last_failure` 字段**没有注入 prompt**
- 验证器：`file_exists` / `file_contains`(支持正则) / `command_succeeds` / `output_contains` / `dir_created`

### 关键差距
1. **无经验沉淀**：失败教训只以字符串存 `regression.yaml`，不可检索、不复用
2. **无相似检索**：Agent 遇到新任务，不知道历史上有过相似的失败
3. **无本体进化**：skill / 系统提示词不会因为失败而改进
4. **评测集固定**：任务集不会随新失败模式自动扩张，有"过拟合评测集"风险

---

## 1. 总目标：三条进化回路

```
┌────────────────────────────────────────────────────────────┐
│  回路一：运行时经验注入（任务级）                            │
│  失败 → 提炼结构化经验 → 向量库 → 相似任务时检索注入上下文    │
│                                                            │
│  回路二：离线本体进化（能力级）                              │
│  失败样本 → 进化 Agent → 修改 skill / 系统提示词 → 回归验证   │
│                                                            │
│  回路三：评测集自进化（评测级）                              │
│  新失败模式 → 自动生成新任务/新校验器 → 评测集变强           │
└────────────────────────────────────────────────────────────┘
```

三条回路对应三个不同层级，可独立推进、互为支撑。

---

## 2. 回路一：运行时经验注入（独立向量库）

### 2.1 设计目标
把"任务怎么失败的、怎么改能过"沉淀成**可检索的结构化经验**，遇到相似任务时自动注入上下文，让 Agent **不再踩同一个坑**。

### 2.2 存储设计：独立向量库（与现有记忆解耦）
- **不**复用现有分层记忆（避免污染用户/项目记忆）
- 单独建**失败经验库**：`benchmarks/experience/` 或独立向量库文件

**每条经验结构（结构化字段）**：
```yaml
id: exp-0001
task_name: refactor-fibonacci          # 来源任务
domain: refactor
failure_snapshot:                       # 失败时的状态
  verify: "file_contains ... 未匹配"     # 挂了哪个校验器
  detail: "缺失 parse 函数"              # 失败详情
  agent_output_preview: "..."           # Agent 当时输出（截断）
lesson: "重构时须保留原公开函数签名"       # 提炼出的教训
correct_strategy: "先读旧文件 → 新函数改名 → 保留兼容层"
embedding: [0.12, -0.05, ...]           # 向量，用于相似检索
created_at: 2026-08-18
```

### 2.3 检索注入流程
```
Agent 拿到新任务
   ↓
1. 任务 description / 校验器 spec 编码成 query
   ↓
2. 向量库相似检索（top-k，设相关度阈值）
   ↓
3. 命中的经验注入上下文（限量，如 ≤3 条）
   ↓
4. Agent 完成任务 → 若失败 → 提炼新经验 → 写回向量库（闭环）
```

### 2.4 关键设计点（要写进实现）
- **检索时机**：任务开始时注入，配合现有**记忆召回链路**（`memory/recall.py` 的 selector 思路）
- **数量/相关度阈值**：注入太多占上下文且引入噪声；要设 `top-k` 上限 + 相关度下限（呼应项目记忆的净收益校验）
- **经验质量校验**：只有"校验器确认失败的样本"才沉淀，避免模型误判产生"错误经验"
- **注入格式**：作为提示词里的"历史经验参考"段落，标注来源，让 Agent 知道这是参考而非硬约束

### 2.5 落地方式（对接现有代码）
- 复用 `_verifiers.py` 的失败详情 → 作为经验 `failure_snapshot`
- 复用 `runner.py` 的 `add_to_regression` 触发点，加一步"同步提炼经验进向量库"
- 向量库可用轻量方案（如 `chromadb` / `faiss` / 简单 numpy 余弦），避免重依赖
- 注入逻辑挂到 Agent 主循环 / 评测的 `_build_prompt`

---

## 3. 回路二：离线本体进化（进化 Agent）

### 3.1 设计目标（写设计方向，不写代码）
通过**另一个"进化 Agent"**，离线分析失败样本，**修改 skill / 系统提示词**，让 Agent 本体能力提升——而不是每次运行时临时喂经验。

### 3.2 闭环流程
```
线上/评测失败样本集
   ↓
1. 聚合：按领域/校验器/失败模式聚类
   ↓
2. 进化 Agent 分析：
   - 根因定位（是任务理解？工具调用？还是能力边界？）
   - 提炼"该改进什么"：加 skill？改提示词？加工具？
   ↓
3. 生成修改提案（skill 内容 / 系统提示词补丁 / 新工具说明）
   ↓
4. 暂存到候选分支（不直接上线）
   ↓
5. 全量回归验证（跑完整评测集 + 回归集）
   ↓
6. 通过则合并生效，失败则回滚
```

### 3.3 关键设计点
- **与现有回归闭环闭环**：离线改 skill 后，**必须跑全量回归**——这正是现有 `runner.py` 的价值：防止"修好 A 弄坏 B"
- **改动不可逆风险**：进化 Agent 是自动改代码/配置，必须**隔离 + 可回滚**（候选分支 / 配置版本化）
- **人类审批（可选）**：高影响改动（改系统提示词）可先出 diff 让人审，低影响（新增 skill 说明）可自动合并
- **进化 Agent 自身的反馈**：它的改动效果由"下一轮回合通过率变化"衡量，形成 meta 闭环

### 3.4 与现有模块衔接
- `skills/` 目录（`builtins/`、`loader.py`、`parser.py`）→ 进化 Agent 的输出目标是生成/修改 skill 文件
- `prompts.py` / 系统提示词 → 进化 Agent 可输出提示词补丁
- 回归验证 → 复用 `benchmarks/runner.py`

---

## 4. 回路三：评测集自进化（补充，可选）

> 这是本项目最独特、也最容易出彩的点，建议作为第三阶段。

### 4.1 思路
固定评测集会有"过拟合评测集"风险——Agent 可能恰好学会了那 16 题。要让评测集**跟着真实失败模式一起进化**：
- 线上/生产出现**新的失败模式**（不在现有任务集里）
- 自动**生成新任务 + 新校验器**，加入评测集
- 让评测覆盖范围越来越接近真实世界

### 4.2 闭环
```
新失败模式 → 转成新任务（description + verify 校验器）
→ 加入评测集 → 纳入回归集 → 后续每次改动都覆盖到
```

### 4.3 关键设计点
- 生成的新任务要有**可写死的验收标准**（否则测不出客观结果）
- 需要防"评测集无限膨胀"：设上限 / 淘汰低价值任务

---

## 5. 实现优先级建议

| 阶段 | 内容 | 复杂度 | 收益 |
|------|------|--------|------|
| P1 | 回路一：向量库沉淀 + 相似检索注入 | 中 | 高（直接减少重复犯错） |
| P2 | 回路三：评测集自进化（新任务生成） | 中 | 中高（让评测不僵化） |
| P3 | 回路二：离线进化 Agent 改 skill | 高 | 高（真正改变本体） |

建议先做 P1（成本低、见效快、面试也最好讲），再做 P2，最后 P3。

---

## 6. 面试讲法升华（可选，面试后也可用）

如果面试官追问"评测闭环之后怎么更进一步"，可讲：
> "当前闭环解决的是'防退化'（回归重测）。我的下一步是两条进阶：**运行时经验注入**——把失败提炼成结构化经验进向量库，相似任务时检索注入，解决'重复犯错'；**离线本体进化**——用另一个进化 Agent 分析失败样本，离线改进 skill 和系统提示词，再用全量回归验证，解决'能力本身提升'。本质是把评测从'打分工具'变成'进化引擎'。"

---

## 7. 待定/需面试后再定

- [ ] 向量库选型（chromadb / faiss / 轻量自实现）
- [ ] 经验注入的最大条数与相关度阈值
- [ ] 进化 Agent 的模型选择（是否用更强模型做离线分析）
- [ ] 评测集容量上限与淘汰策略
- [ ] 是否需要人类审批 gate（针对系统提示词改动）
