# 多 Agent 问题地图

> 定位：**本项目的多 Agent 相关问题的完整清单 + 归类 + 根因归纳**。用于排期修复、面试备战。
> 来源：2026-09-15 项目实战梳理，逐条对照代码核实。
> 原则：只写**核实过**的结论；区分"设计结论"与"待修缺陷"。

**状态图例**：`✅ 已修` / `❌ 未修` / `⏸ 暂不处理`

---

## 0. 先回答一个问题：都是多 Agent 的问题吗？

**不是。** 大致 8 : 2 分：

| 类别 | 条数 | 说明 |
|------|:---:|------|
| **多 Agent 特有** | ~12 | 通信 / 共享状态 / 生命周期 / 控制面 |
| **通用层** | ~3 | 工具契约 / 异步实现质量 —— **单 agent 项目一样会有** |

---

## 1. 划分标准（两个正交维度）

| 维度 | 取值 |
|------|------|
| **是否多 Agent 特有** | 多 Agent 特有 / 通用层 |
| **归属子系统** | 通信 · 状态 · 生命周期 · 控制面 · 工具契约 · async |

**四大部分总览**：

| 部分 | 多 Agent 特有？ | 归属 |
|------|:---:|------|
| 一、Agent 间通信 | ✅ | 通信 |
| 二、共享状态与通知 | ✅ | 状态 + 通知 |
| 三、生命周期与编排控制面 | ✅ | 编排 |
| 四、通用层 | ❌ | 工具契约 / async |

---

# 第一部分 · Agent 间通信 ✅ 多 Agent 特有

## 1.1 寻址（命名空间）

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| **lead 收件箱地址不一致** | 队友写固定键 `"lead"`，lead 消费用真实 `agent_id` → **两个不同文件**；且 `"lead"` 从未注册进名称表 → 按名字发消息**必然失败**。消息**永久滞留、无人消费** | ✅ |
| **名称表是进程级全局** | `AgentNameRegistry` 只有一个进程级 `name → agent_id` 字典，**不按 team 隔离**；而队友名的唯一性**只在团队内检查** → 跨 team 重名**互相覆盖**。后果：在 team A 发 `SendMessage(to="alice")` 解析到 team B 的 alice，却写进 **team A 的邮箱** → **静默错投 + 假成功** | ❌ |
| **队友收件箱键用「名字」而非 `agent_id`** | `spawn_inprocess` 的注入/待命轮询读 `<name>.json`，而发送方经名称表解析后写 `<agent_id>.json` → **两个文件**，发给队友的消息**永久滞留**。与 lead 收件箱问题**同源**（同为地址不一致） | ✅ |

> **共性**：都是"**按名字寻址**"的问题 —— 命名空间归谁管、是否按 team 隔离、是"名字→id"还是"名字→收件箱键"。

**已修的收口方式**：引入唯一常量 `LEAD_INBOX`，并把**收件人解析收敛成一份**（`TeamManager.resolve_recipient` + `deliver`），`SendMessage` 与任务通知共用，杜绝两套地址逻辑再次漂移。队友侧同理：**收件箱键统一为 `agent_id`** —— 参数改名 `inbox_key`、把"键是 `agent_id` 不是名字"这条不变量写进模块 docstring，并加回归测试（`TestTeammateInboxKeyConsistency`；**旧写法下该测试会失败**）。

## 1.2 语义一致性（一个语义多处实现）

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| **shutdown 两种表达 + 生产路径没接** | 关闭请求有两种等价表达（结构化字段 `message_type="shutdown_request"` / 内容前缀 `[shutdown]`），但**只有前缀被识别**；更糟的是**生产路径（`TaskManager` 待命轮询）里根本没有 shutdown 判定** → 关闭请求被当成"新一轮任务"，**队友反而被唤醒继续干活** | ✅ |

> **共性**：**同一语义只能有一个定义处**。

**已修的收口方式**：语义下沉到 `mailbox`（`is_shutdown_request` 同时认两种表达 + `partition_shutdown` 统一拆分），并把判定**接进真正的生产路径**。

## 1.3 通道有效期 ⭐（容易漏掉的一个维度）

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| ~~**消息持久 ≠ 可达**~~ | 队友的待命轮询窗口原本是**固定的 60 次 × 1 秒**，窗口过后**没有任何东西再轮询它的收件箱** → 消息静默滞留、工具却返回"发送成功"（**假成功**）。**已修**：队友改为**长驻**——轮询到**团队被删除或收到 shutdown** 为止，窗口消失、假成功消失 | ✅ |

> **⭐ 设计结论**：邮箱是**持久文件**，但**消费它的循环有生命周期**。
> **持久 ≠ 可达**。设计时必须显式定义"**收件人不在时怎么办**"（排队等它回来 / 直接报错 / 超时兜底）。
> 否则就是**静默丢消息 + 假成功**，比直接失败更难排查。

---

# 第二部分 · 共享状态与通知 ✅ 多 Agent 特有

## 2.1 共享状态的一致性

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| **任务板无锁 + 写前不重载** | 共享状态是**多实例写同一个文件**。原实现既**没有文件锁**，`create()` 又**不重新加载**（直接用陈旧内存缓存覆盖落盘）→ 多实例场景**丢更新**、ID 冲突 | ✅（加固） |
| **邮箱锁拿不到时静默继续** | `Mailbox._with_lock` 在 10 次重试**全部因锁冲突失败**时，既不抛错也**没有真正持有锁** → **无锁读-改-写**。与任务板改后的策略（**拿不到锁就抛错**）**不一致** | ❌ |

> **⭐ 一致性策略应当统一**：**宁可失败，也不静默丢数据。**
> **注意**：任务板这一条在**当前可执行路径**下不易触发（队友都是同进程、共用同一实例），性质是**防御性加固 + 为跨进程形态铺路**，不是正在发生的 bug。

## 2.2 状态与信号的缝合

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| **任务板变更不产生通知** | 任务板是**纯 pull**（有人去查才看到）。创建任务并指派给某人后，**被指派人永远不会知道** | ✅ |
| **重复指派会重复通知** | 只要传入的 `assignee` 非空就发通知，**不比较是否真的变了** → 模型"改状态时顺手带上同一个 assignee"就会重复通知 | ✅ |
| **更新侧缺"排除自己"** | 创建侧有 `assignee != self` 的排除，更新侧没有 → **指派给自己也会通知自己**（同一语义两处实现的首次漂移） | ✅ |
| **`assignee` 未归一化** | `assignee` 原样存自由文本，而 `TaskList(assignee=...)` 是**精确相等**比较 → `alice` / `alice-id-1` / `Alice` 互相查不到（通知不受影响，但"存"与"查"不是同一套寻址） | ❌ |
| **`_load()` 静默吞掉损坏** | 解析失败直接 `return`、保留旧内存状态 → `get()`/`list_tasks()` 返回**过期数据且不报错**，与 `_with_lock` 的"宁可失败也不丢数据"自相矛盾 | ❌ |

**已修的收口方式**：指派 / 改派时**主动 push 一条邮件**给负责人 —— **状态走任务板（pull）、信号走邮箱（push）**，两套机制才算缝合。
（更新侧）"是否真的变了"与"是否指派给自己"两条规则**收敛到同一处判断**（`TaskUpdateTool.execute` 的 `should_notify`）；其中**变更判定由 `SharedTaskStore.update()` 在锁内完成**，经 `TaskUpdateResult` 带出 —— 放到锁外"先读再写"会有 **TOCTOU 窗口**。

## 2.3 两种协调范式的定位（设计结论）

| | **邮箱** | **任务板** |
|--|---------|-----------|
| 范式 | **消息传递**（push） | **共享状态**（pull） |
| 文件粒度 | **每 agent 一个**（`mailbox/<id>.json`） | **每 team 一个**（`tasks.json`） |
| 拓扑 | **定向**（点对点） | **全共享** |
| 形态 | **event**（事件流） | **state**（状态快照） |
| 适合 | 需要判断的自由内容（协商、讨论） | 必须可靠的结构化状态（谁在做、做到哪） |

**⭐ 两条结论**：
1. **两者正交，可以也应该并存**（类比：Erlang 进程有 mailbox，同时有 ETS 共享表；Redis 有 pub/sub，同时有 key）。
2. **唯一铁律：同一份事实只能有一个权威来源**（不能双写）。

---

# 第三部分 · 生命周期与编排控制面 ✅ 多 Agent 特有

## 3.1 队友存活 / 唤醒
→ 同 **1.3**（唤醒窗口）。按文档结构可归此，也可并入通信。

## 3.2 生命周期状态同步

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| ~~**CLI 模式队友永不 idle → `TeamDelete` 永远失败**~~ | 只有 TUI 入口会调 `on_teammate_completed` → `set_member_idle`，CLI（`-p`）入口不标记 idle；而 `TeamDelete` 有守卫"有活跃成员则拒绝" → 团队永远删不掉。**已修**：把空闲标记**下沉到生产方**（`TaskManager._run_background` 自己调 `on_teammate_completed`）→ **所有入口自动一致**，新增入口也不会漏 | ✅ |
| ~~**CLI 模式漏报后台任务完成**~~ | `__main__.py` 的 `notification_fn` 挂的是 `drain_mailbox_only`（只含团队邮箱），而同样已定义好的 `drain_notifications`（含 `<task-notification>`）**从未被使用** → CLI 模式下后台任务完成不回投给模型。**已修**：改用 `drain_notifications` | ✅ |

> **根因模式**：**两条入口对同一个生命周期事件处理不一致**（TUI 做了、CLI 没做）。

## 3.3 编排控制面缺失

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| ~~**`TaskStop` 不存在，但系统提示词明确承诺它**~~ | coordinator 的系统提示词写着"**TaskStop** — Stop a running worker"并教它使用；但**没有任何实现** → coordinator 无法停止 worker。**已修**：新增 `TaskStopTool` + `TaskManager.cancel_by_agent`（`agent_id → task_id` 映射），并在 TUI / CLI 两个入口注册（coordinator 白名单本来就含它） | ✅ |

> 这是"声明了但没接线"的**最尴尬一例**：**承诺写进了系统提示词**。

## 3.4 未接线的执行形态

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| **pane（tmux/iTerm2）队友** | ① 探测函数 `detect_backend()` **无条件返回 in-process**，真正的 pane 探测**只被测试调用** → 面板分支**永远不进**；② 传给子进程的三个环境变量（`CODING_AGENT_TEAM_NAME` 等）**全仓无人读取** → spawn 出来的进程**不知道自己是个队友**；③ 该分支里 `sub_agent` 建好（含 teammate 工具集）却**从未 launch**；④ 日志写"falling back to in-process"，**实际直接返回错误，没有回退** | ⏸ |
| **worktree 隔离子 agent** | 隔离分支读的是 **agent 定义文件里的 `isolation`**（不是工具参数），而四个内置 agent **都没设** → 默认不可达。（用户自写定义可以触发） | ⏸ |

> **准确说法**：跨终端队友是**未完成的特性**，不是可用能力 —— **面试时不要声明支持**。

---

# 第四部分 · 通用层 ❌ **不是**多 Agent 问题

## 4.1 工具契约（"声明了但没接线"）

| 问题 |
|------|
| `AgentToolParams.description` **是必填字段，但从未被读取** → 强迫模型**白花 token** 写一个被丢弃的字段 |
| `AgentToolParams.isolation` **在 schema 里声明，但从未被读取** → 模型传了也没用 |

## 4.2 异步实现质量

| 问题 |
|------|
| 邮箱与任务板的文件锁都用**同步 `time.sleep`** 做退避，而调用方是 `async def execute`，且这些工具标了 `is_concurrency_safe=True` → 竞争时会**阻塞整个事件循环**（最长约 1 秒）。属于既有模式，新代码沿用了 |

---

# 第五部分 · ⭐ 元层归纳（最值钱的一层）

## 5.1 贯穿全部问题的两个根因模式

### 模式 A：**声明了但没接线**（4 例）

| 声明处 | 缺失的接线 |
|-------|-----------|
| 工具文档教队友发给 `"lead"` | 名称表里**没注册** `"lead"` 别名 |
| `SendMessage` 支持 `shutdown_request` | 判定**不认这个字段**；且生产路径**没有判定** |
| coordinator 提示词承诺 `TaskStop` | **没有这个工具** |
| 工具参数 `description` / `isolation` | `execute()` **从未读取** |

### 模式 B：**同一语义多处实现**（2 例）

| 语义 | 两处实现 |
|------|---------|
| 收件人地址解析 | `SendMessageTool` 一套、`TeamManager` 一套 |
| shutdown 判定 | `spawn_inprocess` 一套、`task_manager` 一套（且生产用的那个没有） |

## 5.2 由此得到的两条规矩

> **① 同一语义只能有一个定义处。**
> **② "声明了"不等于"接线了"** —— 参数 / 文档 / 提示词写了，**必须有代码路径兜住**。

## 5.3 可复用的验证套路

> **① 找出同一语义的所有实现处 → ② 确认真实代码里哪条路是"活的" → ③ 最小复现 → ④ 反证**

**为什么第 ④ 步是灵魂**：多数人只会说"我怀疑有 bug"；**能反证的才叫定位** —— 即"把可疑点改对之后问题立刻消失"，才能排除"机制本身也坏了"。

---

# 第六部分 · 与现有文档的映射

| 现有 / 建议文档 | 应覆盖 |
|----------------|-------|
| 《多 Agent **通信**设计》→ `multi-agent-communication.md` | 第一部分 + 第二部分（另含拓扑 / 传输 / 语义 / 生命周期四大板块 + 历史问答） |
| 《多 Agent **生命周期与编排**》（建议新建） | 第三部分 |
| 《**已知问题 / 工程质量**清单》（建议新建） | 第四部分 + 第五部分 |

---

# 第七部分 · 面试讲法（1 段，可直接背）

> "我对多 Agent 部分做过一轮**同类 bug 的普查**，最后归纳出两个反复出现的根因模式：
>
> 第一个是 **'声明了但没接线'** —— 参数声明了代码没读、文档教了实现没做、**系统提示词承诺了工具却不存在**。这类 bug 最阴险，因为**看起来一切正常**。
>
> 第二个是 **'同一语义多处实现'** —— 比如收件人地址解析有两套、关闭指令的判定有两套。它们一开始一致，**演化后就漂移**，表现成'发得出、不生效'。
>
> 由此我定了两条规矩：**同一语义只能有一个定义处**；**'声明了'不等于'接线了'**，必须有代码路径兜住。
>
> 验证上我坚持四步：**找多处实现 → 确认真实代码里哪条路是活的 → 最小复现 → 反证**。第四步最关键——**把可疑点改对之后问题立刻消失**，才能排除'机制本身也坏了'，否则只是猜。
>
> 另外我还发现一个通信设计里**容易被忽略的维度：消息通道的有效期**。我把消息做成持久化文件，一度以为'持久 = 不会丢'；但**文件在、消费者不一定在** —— 收件方是个有生命周期的轮询循环。所以我把结论定成 **'持久 ≠ 可达'**，必须显式定义'收件人不在'时的行为。"

---

## 附：修复优先级建议

| 优先级 | 项 | 理由 |
|:---:|----|------|
| ~~**P0**~~ ✅ | ~~3.2 CLI 模式队友永不 idle~~ | 已修（`80f53e7`）|
| ~~**P0**~~ ✅ | ~~1.3 / 3.1 唤醒窗口 + 假成功~~ | 已修（`80f53e7`）|
| ~~**P0**~~ ✅ | ~~3.3 `TaskStop` 缺失~~ | 已修（`80f53e7`）|
| ~~**P0**~~ ✅ | ~~CLI 漏报后台任务完成（同源）~~ | 已修（`80f53e7`）|
| **P1** | 1.1 名称表跨 team 串台 | 多 team 场景**静默错投** |
| **P1** | 2.1 邮箱锁静默无锁 | 与任务板策略不一致，**统一为"失败优于丢数据"** |
| ~~**P1**~~ ✅ | ~~2.2 重复通知~~（含"指派给自己"） | 已修：锁内判定变化 + 排除自己 |
| **P2** | 2.2 `assignee` 未归一化 | `TaskList` 过滤**静默漏查**（存与查不是同一套寻址） |
| **P2** | 2.1 `_load` 静默吞掉损坏 | 返回过期数据不报错，与"宁可失败不丢数据"矛盾 |
| **P2** | 4.1 `description` / `isolation` 未接线 | 白花 token / 参数无效，小改 |
| **P2** | 4.2 锁用同步 sleep | 既有模式，可后续统一改异步退避 |
| **⏸** | 3.4 pane / worktree 未接线 | 明确标注为未支持即可，暂不投入 |

---

## 附二：修复记录（git）

| 提交 | 内容 |
|------|------|
| `463ae21` | lead 收件箱地址统一（唯一定义 `LEAD_INBOX` + 收件人解析收敛一份）；shutdown 语义收敛到唯一定义处（两种表达都认）并接进生产路径 |
| `340bf90` | 任务板一致性（文件锁 + 原子写 + **写前重载**）；指派/改派 **push 通知**；投递收敛（`TeamManager.deliver`） |
| `80f53e7` | 队友空闲标记**下沉到生产方**（修 CLI 模式 `TeamDelete` 失败）；队友改**长驻轮询**（消除 60s 窗口与假成功）；实现 `TaskStop`；CLI 入口改用 `drain_notifications`（修漏报后台任务完成） |
| `f54f4e0` | **① 队友收件箱键统一为 `agent_id`** —— `spawn_inprocess` 的注入/轮询不再用队友名字；参数改名 `inbox_key`；模块 docstring 写死该不变量；回归测试 `TestTeammateInboxKeyConsistency` |
| `f54f4e0` | **② 任务板指派通知修正** —— 只在 `assignee` **真的变了**且**不是指派给自己**时才 push（原来只判断非空）；`SharedTaskStore.update()` 改为返回 `TaskUpdateResult`、变更在**锁内**判定；`TaskUpdateTool` 增加 `agent_name`；回归测试 `test_task_update_same_assignee_does_not_renotify` / `test_task_update_assign_to_self_does_not_notify` |

**贯穿三次修复的同一条原则**：
> **同一语义只能有一个定义处；能靠"位置 / 结构"消除遗漏，就不要靠"每个调用方记得"。**
