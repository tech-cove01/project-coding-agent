# Coding Agent & Agentic RAG 作品集展示页 —— 实施方案

> 目标：把两个项目做成可公开访问的 GitHub Pages 静态页面，给 HR / 面试官看。
> 技术约束：GitHub Pages 只支持**纯静态文件**（HTML/CSS/JS），不能跑后端、不能用 API key。
> 本文档是给"负责做前端页面的模型"看的完整需求说明。

---

## 一、整体结构（建议）

一个仓库放所有页面（或两个仓库分别放两个项目），建议结构：

```
coding-agent-display/            ← 仓库根（GitHub Pages 站点）
├── index.html                   ← 总览页：我是谁 + 技能 + 两个项目入口
├── css/
│   └── style.css                ← 全站统一样式
├── coding-agent/
│   └── index.html               ← Coding Agent 项目展示页
└── rag-project/
    └── index.html               ← Agentic RAG 项目展示页（放 Dify 原型图）
```

部署：仓库 Settings → Pages → 选分支 main / (root) → 访问 `https://<用户名>.github.io/<仓库名>/`

---

## 二、总览页 index.html 核心内容

1. **顶部 Hero**：名字 + 一句定位（如"AI 应用开发工程师，专注大模型 Agent 与 RAG"）
2. **专业技能标签**：Python / FastAPI / LangGraph / MCP / Multi-Agent / RAG / Redis / Milvus / Docker / Git
3. **两个项目卡片**：各一个卡片，点进去看详情
   - Coding Agent（2026.06 — 至今）
   - Agentic RAG（2026.04 — 2026.06）
4. **联系方式**：GitHub / 邮箱

---

## 三、项目展示页通用结构（两个项目复用同一套模板）

每页固定五段式：

### 1. 项目简介（Hero）
- 项目名 + 一句话定位
- 技术栈标签（复用简历）
- 时间 + "独立开发"标识

### 2. 核心亮点（3~5 条，卡片式）
每条 = **小标题 + 2~3 句说明 + 关键数字加粗**

### 3. 架构说明（可选，有图放图，没图用文字+分层列表）
- 文字描述模块分层（Agent 层 / 工具层 / 记忆层 / 存储层）
- 有架构图就放图

### 4. 截图 / 原型图（重点）
- 有真实截图最加分
- **Agentic RAG 项目**：放 Dify 原型图（你已有一个）
- **Coding Agent**：放终端运行截图（需先补拍）

### 5. 底部：返回总览 / 仓库链接

---

## 四、Coding Agent 项目页内容（直接复用简历）

**项目描述**：
> 基于 ReAct 与 Plan 模式双驱动的轻量级终端 Coding Agent，具备分层记忆、任务级断点续跑、多 Agent 协作、MCP 工具生态与自进化评测能力，支撑自动化编码、跨文件重构与复杂代码仓智能问答任务。

**技术栈**：Python / MCP / ReAct / Plan / Multi-Agent / Git Worktree

**亮点（4~5 条，带量化）**：

1. **分层记忆与任务级断点续跑**
   两层渐进式上下文压缩（近期原文保留 + 结构化摘要，含净收益校验）；打通"LLM 提取→记忆块解析→按类型路由→索引写入"持久化链路，修复自动记忆"只收集不落盘"缺陷。实现任务级 Checkpoint，每轮工具执行后落盘完整对话，中断后恢复上下文继续推进。

2. **端到端自进化评测闭环**
   基于 16 个真实编码任务（7 领域，easy/medium/hard）的评测流水线，任务拆分为可校验验证器（文件产出/内容匹配/命令退出码），驱动 Agent 隔离执行并打分；失败案例自动进回归集并下一轮自动重测——两轮实测失败数 6→5，通过率 62.5%→77.3%。

3. **多 Agent 协作与 Worktree 文件级隔离**
   coordinator 模式编排多子 Agent（规划→并行研究→综合→实现→独立验证），各子 Agent 运行在独立 Git Worktree 实现文件级隔离；直接解析 .git 免起子进程快速恢复（100ms→1ms），symlink 共享大依赖，三重守卫自动清理陈旧工作树；大型重构效率提升约 60%。

4. **五层安全隔离与自主执行**
   五层权限确认（危险命令拦截/路径沙箱/规则引擎/会话级放行/模式兜底），权限确认弹窗从平均 30 次降至 5 次以内；支持内核级隔离沙箱兜底。

5. **MCP 工具延迟加载与上下文优化**
   MCP 延迟加载按需加载工具描述，工具描述 Token 占用减少 74%；支持 stdio 与 HTTP 双传输。

---

## 五、Agentic RAG 项目页内容（直接复用简历）

**项目描述**：
> 基于 LangGraph 的 Agentic RAG AIOps 平台，通过 RAG 问答 Agent 与 Plan-Execute-Replan 诊断工作流，打通知识库、Prometheus 指标、日志与 MCP 工具，实现问题自动应答与故障智能排查，并靠经验库闭环与多级缓存持续优化准确率与响应性能。

**技术栈**：Python / FastAPI / LangChain / LangGraph / MCP / Redis / Milvus / SQLite / vis-network / Dify

**亮点（5 条，带量化）**：

1. **多智能体工作流编排**
   LangGraph 构建 RAG 问答 + Plan-Execute-Replan 双工作流，按需选路；"全局规划—按需检索—智能诊断—建议生成"闭环，引入 Replanner / 最大步数 / 收敛阈值，故障响应压缩至约 2 分钟，防推理死循环；工具失败自动指数退避重试。

2. **Agentic RAG 经验自学习闭环**
   MCP 将 Prometheus 指标 / 日志 / 私有文档封装为 Agent 工具，打通异构数据孤岛；"处理→沉淀→复用"经验库，诊断完成自动沉淀告警特征/根因/步骤，同类告警直接复用历史经验；"精确匹配 + 向量语义（≥0.85）"双轨去重防经验库膨胀。

3. **多级缓存与长会话性能优化**
   Redis + Milvus 构建"高频 Q&A 库—会话语义缓存—大模型"三级缓存，问题归一化 + 相似度匹配减少重复调用；重复请求命中率 100%，**缓存命中时**响应由 28.6s 降至 4.3ms（时延降 99.98%）。

4. **智能降级与统一可观测**
   "LLM 智能诊断—规则模板降级"双路径，模型接口异常时基于真实告警/指标/日志自动生成诊断报告；统一监控缓存命中率/请求时延/降级率/知识库容量/Redis、Milvus 健康状态。

5. **运维知识图谱可视化**
   SQLite 轻量图谱（8 服务 + 13 依赖边，6 类关系），BFS 1~2 跳检索，诊断经验自动写回图谱；vis-network 实现节点-关系可视化。

---

## 六、素材需求（做页面前先准备）

| 素材 | 用途 | 状态 |
|------|------|------|
| **Dify 原型图** | Agentic RAG 项目页截图区 | ✅ 已有（需提供文件/图片） |
| **Coding Agent 终端截图** | Coding Agent 项目页截图区 | ❌ 需补拍（启动 agent 跑一个任务截屏） |
| **架构图**（可选） | 两个项目的架构说明区 | ❌ 可选，可手绘或用文字分层代替 |
| **头像/Logo**（可选） | 总览页 Hero | ❌ 可选 |

---

## 七、样式建议（给前端模型的参考）

- **风格**：深色科技感（呼应 AI/Agent 主题），或简约白底（更通用）
- **配色**：主色一个（如蓝/紫），强调色一个，避免花哨
- **字体**：系统字体栈 + monospace 用于代码/技术栈标签
- **亮点卡片**：每条一卡，数字用大号加粗，一眼看到量化
- **响应式**：手机也能看（HR 可能用手机）
- **交互**：克制，页面滚动展示即可，不要炫技动效
- 参考风格：Claude / Cursor 官网那种"功能分区 + 数据卡片"布局

---

## 八、部署步骤（做前端的模型要照做）

1. 建仓库（如果还没有）：`coding-agent-display` 和 `Agentic-RAG-display`（或一个总仓）
2. 按上面的结构放好 HTML/CSS/素材
3. 推到 GitHub
4. 仓库 Settings → Pages → Source 选 `main` 分支 `/ (root)`
5. 等 1~2 分钟，访问 `https://<用户名>.github.io/<仓库名>/` 验证

---

## 九、给做前端的模型的指令（可直接复制）

> 请根据本文档，为" Coding Agent "和" Agentic RAG "两个项目各搭建一个纯静态 GitHub Pages 展示页，加一个总览页。要求：
> 1. 用纯 HTML/CSS/JS，不用框架，能直接在 GitHub Pages 部署
> 2. 复用文档里的项目描述和亮点文案（含量化数字）
> 3. 深色科技感设计，亮点用卡片式，数字加粗突出
> 4. 预留"截图/原型图"的占位区域（图我后续补）
> 5. 响应式布局，手机可看
> 请先输出完整代码，并说明文件结构和部署步骤。
