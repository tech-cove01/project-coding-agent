# 多 Agent 通信设计

> **定位**：本项目的多 Agent 通信**设计说明 + 边界判断 + 概念澄清**。与 `multi-agent-issues.md`（问题清单）互补——本文写"**怎么设计的、为什么这么设计**"，那份写"**哪些地方坏了、怎么修**"。
>
> **来源**：2026-09-15 逐行对照代码梳理。
> **原则**：只写**核实过**的结论；区分「设计如此」与「实现漏了」；不确定就标注为待确认。

---

## 0. 速览（TL;DR）

| 维度 | 结论 |
|------|------|
| **形态** | 4 种：一次性子 agent / worktree 隔离子 agent / 进程内队友 / pane 队友（**未接线**） |
| **拓扑** | 混合：**默认星型**（lead 协调）+ **可选网状**（队友直连）+ **共享任务板**（blackboard） |
| **传输** | **文件邮箱**（每 agent 一个 JSON + 手写文件锁），零依赖、跨进程、可 `cat` 调试 |
| **语义** | **at-most-once（邮箱语义）**，不是队列语义 —— `consume()` 标记已读即"读走即弃" |
| **边界** | 同一进程用队列/直调；同机跨进程用文件/SQLite；跨机才轮到 MQ / **A2A** |
| **能力边界** | **管道的结构与权限全在代码**；只有"要不要发、发什么"在提示词 |
| **最大缺口** | pane 队友未接线；跨进程那条路实际走不到 |

**一句话概括**：**这是一套"在同一进程内、用跨进程的形态"实现的通信层** —— 基础设施齐备（邮箱/锁/长驻/唤醒），但跨进程的入口没接上线。

---

# 第一部分 · 先分清对象：四种 Agent 形态

讨论"通信"前必须先分清**说的是哪种 agent** —— 它们的通信方式完全不同。

## 1.1 触发条件是「短路式级联」（顺序即优先级）

`AgentTool.execute` 从上到下判断，**先满足的先赢**：

```python
# coding_agent/tools/agent_tool.py
if p.team_name:                    # ① 队友：最高优先，直接短路
    return await self._execute_as_teammate(p)
...
if isolation == "worktree":        # ② worktree 隔离子 agent
    ...                            #    前台阻塞，跑完 auto_cleanup
is_background = p.run_in_background or definition.background   # ③ 后台
if self._enable_fork:
    is_background = True           #    fork 会强制全部走后台
...
await sub_agent.run_to_completion(p.prompt)   # ④ 前台（默认）
```

## 1.2 四种形态对照

| 形态 | 触发条件 | worktree 隔离 | 邮箱 | 生命周期 | 结果怎么拿 |
|------|---------|:---:|:---:|------|------|
| **一次性子 agent** | 无 `team_name`、无 `isolation` | ❌ | ❌ | 跑完**消失** | **函数返回值** |
| **worktree 隔离子 agent** | `isolation == "worktree"` | ✅ | ❌ | 跑完**消失** | **函数返回值** |
| **进程内队友** | **有 `team_name`** | ✅ | ✅ | **长驻**，反复接活 | 邮箱 + 通知队列 |
| **pane 队友** | `team_name` + backend=tmux/iTerm2 | ✅ | ✅ | 长驻（**未接线**） | 邮箱 |

## 1.3 三个容易考的点

**① `team_name` 判断在最前面** —— 一旦有值就走队友分支，**连 worktree 判断都跳过**（队友在 `_execute_as_teammate` 内部自己建 worktree）。

**② `_enable_fork=True` 强制所有子 agent 走后台** —— fork 出来的是共享父对话前缀的独立 agent，前台阻塞会卡死主循环。

**③ `Agent` 工具标记为不可并发**：

```python
# coding_agent/tools/agent_tool.py
is_concurrency_safe = False
```

配合主循环的分批逻辑：

```python
# coding_agent/agent.py  partition_tool_calls()
safe = tool is not None and tool.is_concurrency_safe and registry.is_enabled(...)
if safe and batches and batches[-1].concurrent:
    batches[-1].calls.append(tc)
else:
    batches.append(ToolBatch(concurrent=safe, calls=[tc]))
```

→ **子 agent 会改文件、有副作用，所以被结构强制串行**。这是"**能用结构约束就不靠提示词**"的又一次落地。

## 1.4 ⭐ 关键结论：worktree 隔离 ≠ 需要通信

第二行（worktree 隔离子 agent）**有隔离、没有邮箱** —— 它证明了：

> **隔离本身根本不需要通信。** 需要通信的是"**长期共事、并行开工**"的队友。

而方向是**反直觉**的：

> **隔离越强，通信需求反而越强。** 正因为队友在各自 worktree 里互相看不见对方的改动，才更需要一条**显式通道**去对齐接口。

| | 解决什么 | 性质 |
|---|---|---|
| **worktree** | 文件冲突、工作区污染 | **物理**隔离（状态） |
| **邮箱** | 信息同步 | **逻辑**连接（信息） |

**两个正交维度，可只选其一。**

---

# 第二部分 · 拓扑：谁跟谁说话

## 2.1 三种协调机制并存

| 机制 | 范式 | 粒度 | 拓扑 | 形态 | 适合 |
|------|------|------|------|------|------|
| **函数返回值** | 调用 | — | 父→子 | — | 一次性子 agent（同进程、阻塞） |
| **邮箱** | **消息传递（push）** | **每 agent 一个** | **定向 / 广播** | **event** | 需要判断的自由内容（协商、对齐接口） |
| **任务板 `SharedTaskStore`** | **共享状态（pull）** | **每 team 一个** | **全共享** | **state** | 必须可靠的结构化状态（谁在做、做到哪） |

**两条设计结论**：

1. **邮箱和任务板正交，可以也应该并存**（类比：Erlang 进程有 mailbox，同时有 ETS 共享表）。
2. **唯一铁律：同一份事实只能有一个权威来源**（不能双写）。

**已做的缝合**：任务板是纯 pull，所以**指派/改派时主动 push 一条邮件**给负责人 —— **状态走任务板、信号走邮箱**。

## 2.2 星型 vs 网状

**代码只提供能力，不强制用法**：

- `to="Alice"` → 定向投递（一对一点对点）
- `to="*"` → 广播

| | 纯星型（全走 lead） | 允许点对点 |
|---|---|---|
| lead 上下文 | 被琐事污染 | **干净，只管决策** |
| 延迟 | 每次交换 **2 跳** | **1 跳** |
| 可追踪性 | ✅ 一处看全 | ⚠️ 要翻多个邮箱 |
| lead 全局视野 | ✅ 完全掌握 | ⚠️ 队友私下聊了啥它不知道 |
| 冲突风险 | ✅ 低（都经 lead 裁决） | ⚠️ 可能与 lead 规划冲突 |

**一句话**：**星型更可控，网状更高效。**

**判断准则**：

| 什么时候用星型 | 什么时候值得点对点 |
|---|---|
| 任务互相独立，不需要对齐 | 任务**有接口/契约要对齐**（前后端对接） |
| lead 能事先把细节定清楚 | 细节**在执行中才浮现**，需来回确认 |
| 团队人数少（2–3 个） | 人数多、交互密集 |
| 更看重可控可追踪 | 更看重效率、防 lead 瓶颈 |

**默认建议：从星型开始。**

## 2.3 ⭐ 最可靠的"星型"是消除通信需求

拆任务时就把接口冻结：

```
lead 明确指定：
  - Alice 实现 POST /login，入参 {username, password}，返回 {token}
  - Bob   按上面这个接口写前端页
```

→ Alice 和 Bob **根本不需要对话**。协调成本从"运行时反复对齐"变成"**规划时一次性确定**"。

> **不禁止通信，而是消除通信需求。**

---

# 第三部分 · 传输：靠什么送

## 3.1 ⭐ 边界决定选型

核心原则：**通信的复杂度取决于「边界」**。

**进程内你白拿四样东西，跨进程全部消失、必须显式重建**：

| 进程内白拿 | 跨进程必须重建 | 谁负责 |
|---|---|---|
| 共享内存（对象引用） | **序列化**（JSON / protobuf） | 协议 |
| 共享类型系统（同一个 class） | **Schema 约定**（双方独立实现） | 协议 |
| 共享生命周期（同进程活着） | 处理"对方**不在 / 崩了 / 很慢**" | 协议 |
| 共享信任（同进程天然可信） | **认证 + 授权** | 协议 |
| 函数返回值 / 异常 | **Task + 状态机 + 错误码** | 协议 |

> **本质**：**协议就是"在网络上重建你在进程内白拿的那些东西"。**

**按边界选工具**：

| 场景 | 该用什么 | 为什么 |
|------|---------|--------|
| **同进程多 agent**（**本项目**） | `asyncio.Queue` / 直接函数调用 / 共享状态 | 共享内存，**零成本** |
| **同机跨进程** | 文件 / Unix Socket / **SQLite** / 本地 broker | 要序列化，但不需要"发现"和"鉴权" |
| **跨机 / 跨组织 / 跨框架** | **A2A**、gRPC、消息中间件 | 发现 + 鉴权 + 长任务语义全都要 |

> ⚠️ `asyncio.Queue` **跨不了进程**（哪怕同机）；一旦跨进程，就已掉进"必须重建四样东西"的世界。

## 3.2 当前实现：文件邮箱

### 为什么用文件而不是内存队列

**根本原因**：队友**设计上**可能跑在 tmux / iTerm2 的**独立进程**里，`asyncio.Queue` 跨不了进程。文件邮箱天然**跨进程、可持久化、可直接打开调试**。

### 目录结构

```
<team_dir>/
├── config.json          # AgentTeam 元信息（name / lead_agent_id / members）
├── tasks.json           # SharedTaskStore 共享任务板
└── mailbox/
    ├── <inbox_key>.json       # 每个收件人一个收件箱（JSON 数组）
    └── <inbox_key>.json.lock  # 配套文件锁
```

### 寻址：收件箱键 ≠ agent_id（**关键设计**）

```python
# coding_agent/teams/models.py
# 邮箱目录按 team 隔离（<team_dir>/mailbox/），因此同一个 "lead" 键
# 在不同团队之间互不冲突。
LEAD_INBOX = "lead"
```

```python
# coding_agent/teams/manager.py  TeamManager.resolve_recipient()
if to == LEAD_INBOX:
    return LEAD_INBOX                      # "lead" → 固定键
return AgentNameRegistry.instance().resolve(to)   # 其他 → 名称表解析成 agent_id
```

**收件箱键有两种**：

| 收件人 | 收件箱键 |
|--------|---------|
| lead | **固定键 `"lead"`**（因为邮箱按 team 隔离，键在团队内唯一） |
| 队友 | **真实 `agent_id`**（经名称表解析） |

> **收件人解析只实现一份**（`resolve_recipient`），投递收敛到 `deliver`，唤醒收敛到 `wake_pane` —— 因为"**同一语义多处实现**"正是之前地址漂移的根因。

### 并发安全：原子创建锁文件

```python
# coding_agent/teams/mailbox.py  Mailbox._with_lock()
fd = os.open(str(lock_file), os.O_CREAT | os.O_EXCL | os.O_WRONLY, 0o644)
#        不存在的才创建成功 —— 原子
except FileExistsError:
    if time.time() - info.st_mtime > 10:      # 超 10 秒判为陈旧锁，自动清理
        lock_file.unlink(missing_ok=True)
    sleep_ms = 5 + random.randint(0, 95)      # 5–100ms 随机退避，避免惊群
```

重试 10 次；整个"读-改-写"包在锁内 —— 防止两个 agent 同时写同一收件箱丢消息。

### 消费语义：at-most-once（**这是"邮箱"不是"队列"**）

```python
# coding_agent/teams/mailbox.py  Mailbox.consume()
def _mark_read(msgs):
    for m in msgs:
        if not m.read:
            result.append(m)
            m.read = True          # ← 先标记已读，再返回
    return msgs
```

| 语义 | 像什么 | 谁 |
|---|---|---|
| **读走即弃**（at-most-once） | **邮件**：打开就没了 | **本项目邮箱** |
| **处理完才确认**（at-least-once） | **工单**：做完才关单 | 消息队列（ack） |

> ⚠️ **崩溃即丢**：若在"标记已读"之后、"送进上下文"之前进程崩溃，该消息**永久丢失**。

### 广播 = 写 N 份（写放大）

```python
def broadcast(self, team_members, message, exclude=""):
    for agent_id in team_members:
        if agent_id == exclude:
            continue
        self.write(agent_id, message)      # N 次落盘
```

### 关键代码路径（生产路径）

| 环节 | 位置 | 说明 |
|------|------|------|
| **发送** | `SendMessageTool.execute` | `to="*"` → `broadcast`；否则 → `TeamManager.deliver` |
| **发送校验** | `SendMessageParams` | `text` 类型**必须带 `summary`**，否则报错 |
| **接收（队友）** | `TaskManager._run_background` 待命循环 | `mailbox.consume(bg.agent.agent_id)` |
| **接收（lead）** | `TeamManager.drain_lead_mailbox` | `mailbox.consume(LEAD_INBOX)` |
| **注入** | `Agent._consume_mailbox` / `notification_fn` | 拼成 user message / system-reminder |
| **上报空闲** | `TeamManager.on_teammate_completed` → `set_member_idle` | 写 `LEAD_INBOX` |

**注入形式有两套（别混）**：

| 谁 | 形式 | 为什么 |
|---|---|---|
| **队友收到的消息** | **`add_user_message`** | 对队友来说就是"用户下的新指令" |
| **lead 收到的团队通知** | **`add_system_reminder` + XML** | 系统转达的旁路通知 |

### 队友长驻循环（生产路径在 `TaskManager`）

```python
# coding_agent/agents/task_manager.py
_IDLE_POLL_SECONDS = 1.0

team_manager.on_teammate_completed(bg.agent.agent_id)
while True:
    await asyncio.sleep(_IDLE_POLL_SECONDS)
    if team_manager.get_team(bg.agent.team_name) is None:
        break                                   # 团队被删 → 退出
    msgs = mailbox.consume(bg.agent.agent_id)
    keep, has_shutdown = partition_shutdown(msgs)
    if has_shutdown:
        bg.status = "stopped"; break            # 收到 shutdown → 退出
    result = await bg.agent.run_to_completion(prompt)   # 否则当新一轮任务
    team_manager.on_teammate_completed(bg.agent.agent_id)
```

**设计要点**：**刻意不设固定时长上限** —— 一旦窗口过期，发给队友的消息会**静默滞留**，而 `SendMessage` 仍返回"已发送"（**假成功**）。

### `shutdown` 语义收敛到唯一定义处

```python
# coding_agent/teams/mailbox.py
def is_shutdown_request(msg):
    if msg.message_type == "shutdown_request":   # ① 结构化声明
        return True
    return msg.content.strip().startswith("[shutdown]")   # ② 文本前缀
```

两种表达**都认**，且**所有取消息的循环都走 `partition_shutdown`** —— 避免"发得出、不生效"。

## 3.3 当前实现的三条硬约束

| 约束 | 后果 |
|------|------|
| **轮询**（无推送） | 1 秒延迟（`_IDLE_POLL_SECONDS`） |
| **无投递保证** | 无 ack、无重试；标记即读走 → 崩溃即丢 |
| **无发现/鉴权** | 靠进程内单例名称表；跨进程即失效 |

> **准确说法**：**它是一个"为跨进程而设计的协议雏形"，但没建完。** 缺的正好是 3.1 表中"跨进程必须重建"的那几样。

## 3.4 演进方向：SQLite（**接口不变、实现替换**）

| 层 | 现在 | 换 SQLite 后 |
|---|---|---|
| **接口**（`write` / `consume` / `broadcast`） | — | ✅ **完全不变** |
| **存储** | JSON 文件 + 手写锁 | 一张表 + 事务 |
| **语义** | at-most-once | ⬆️ 可升级 at-least-once（ack） |

→ **对上层调用方零改动。**

### 对手写方案与 SQLite

| 对比项 | JSON 文件邮箱 | SQLite |
|---|---|---|
| 依赖 | 零 | **零**（标准库 `sqlite3`） |
| 原子性 | 手写 `O_CREAT\|O_EXCL` 锁 | **真事务**（WAL） |
| 并发写 | 重试 10 次 + 随机退避，**可能失败** | 内建锁，**不丢** |
| 崩溃安全 | 可能写坏（半个文件） | WAL，**崩溃不损坏** |
| 消费 | 全量读改写整个 JSON | `UPDATE ... WHERE read=0 RETURNING` |
| 复杂度 | O(**所有**消息) | O(**命中**消息) |
| 可调试 | `cat` 可读 | `sqlite3 mailbox.db` 可读 |
| 跨进程 | ✅ | ✅ |

> **你手写的那套锁（10 次重试、5–100ms 随机退避、10 秒陈旧锁检测），本质上就是在手工重造 SQLite 免费给你的东西。**

### 顺带的两个升级

**① 广播从「写 N 份」变成「写 1 次」** —— 规范成两张表：

```sql
messages(id, team, from_agent, content, summary, created_at)   -- 消息体，写一次
deliveries(message_id, to_agent, acked_at)                     -- 投递状态，每收件人一行
```

**"已读"本来就是"每收件人一份"**，用 `deliveries` 表正好对应。**这就是 MQ 的标准做法**（消息 + 每消费者 offset）。

**② 地址 bug 藏不住了** —— 寻址从"文件名"变成 `WHERE to_agent = ?`，`SELECT DISTINCT to_agent` 一眼就能看出孤儿地址。

### ⚠️ 边界提醒

**SQLite 的前提是「同机」**：

| 场景 | SQLite 合适吗 |
|------|:---:|
| 同机跨进程 | ✅ |
| 多机共享网络文件系统（NFS） | ⚠️ **锁不可靠，别用** |
| 跨机器 / 跨组织 | ❌ 这才轮到 MQ / A2A |

## 3.5 MQ 的取舍（为什么**不**上 broker）

| 能力 | 说明 |
|------|------|
| **ACK / 重投** | 崩溃不丢（at-least-once） |
| **消费组** | 多 worker 抢同一队列 |
| **真 pub/sub** | 一次 publish，broker 负责 fanout（对比：本项目 broadcast 是写 N 份） |
| **保留 + 回放** | Kafka 按 offset 重放历史 |
| **背压 / 可观测** | 流控、lag、消费速率 |

| | 文件邮箱 | broker MQ |
|---|---|---|
| 依赖 | **零** | **要跑一个 broker 进程** |
| 运维 | **无** | 部署、监控、升级、磁盘规划 |
| 单机 CLI 适用性 | ✅ | ❌ **杀鸡用牛刀** |
| 可调试 | ✅ `cat` | 要专门工具 |

> **MQ 解决的是"跨机、高吞吐、需要投递保证"的问题。** 本项目是**单机 CLI、消息量极小**，引入 broker 是**拿运维复杂度换不需要的能力**。

### ⭐ MQ 不是唯一能"持久化"的东西

| | 提供什么 |
|---|---|
| **数据库（SQLite）** | 持久化 + **原子性** + 查询 |
| **消息队列（MQ）** | 持久化 + **投递语义**（ack / 重投 / 消费组） |
| **本项目文件邮箱** | 持久化（**只有字节，没有语义**） |

> **需要的是"原子性"，不是"投递保证"** —— 所以答案是**数据库**，不是 MQ。

---

# 第四部分 · 语义层：契约 vs 协议

## 4.1 ⭐ 核心区分：「契约」≠「协议」

| | **契约（Contract）** | **协议（Protocol）** |
|---|---|---|
| 是什么 | 消息**长什么样**（schema / 类型） | 契约 **+ 寻址 + 传输 + 鉴权 + 生命周期** |
| **进程内需要吗** | ✅ **需要** | ❌ **不需要** |
| 跨进程需要吗 | ✅ 需要 | ✅ **需要** |

**本项目已经有"契约"，而且很规范**：

```python
class SendMessageParams(BaseModel):
    to: str
    message: str
    summary: str = ""
    message_type: str = "text"
    metadata: dict[str, Any] | None = None
```

pydantic 强类型 + 校验（`text` 必须带 `summary`），**模型乱填会被拒并促使纠正**。

> **准确说法：本项目有「契约」，没有「协议」—— 因为没跨边界。**

## 4.2 A2A 到底保证什么

**先排除误解**：**A2A 不保证"消息不丢"**。它是**接口与语义契约**，不是传输可靠性层。

它规定这五件**互操作语义**：

| # | 规定什么 | 内容 |
|---|---|---|
| 1 | **能力发现** | **Agent Card** 放在 `/.well-known/agent-card`，声明能力、`url`、`securitySchemes` → **不用事先知道对方是谁就能接入** |
| 2 | **任务抽象 + 生命周期** | 一次协作 = 一个 **Task**，有**状态机**（`Submitted → Working → (Artifact) → Completed / Failed / Canceled`）→ **可查询的有状态实体**，不是"发完不管" |
| 3 | **产物交付** | 结果以 **Artifact** 流式返回 |
| 4 | **异步与长任务** | SSE 流式 + webhook 推送，原生支持 LRO 与**对方不在线** |
| 5 | **企业级安全** | 标准 Web 鉴权（OIDC / OAuth2 / API key），走 HTTPS |

**最关键的是这条设计原则**：

> **Opaque Execution（不透明执行）**：协作时**不暴露**自身内部逻辑、记忆、专有工具；交互只基于**已声明能力**与**共享上下文**。

→ **A2A 的整个前提是「对方是我不认识的、不透明的、跨组织的黑盒」。**

**官方技术栈分层**（A2A 明确不依赖、不偏向任何框架）：

```
A2A        ← 智能体之间通信
MCP        ← 模型连接数据与资源
Agent 框架  ← 构建智能体的工具箱（LangGraph / CrewAI / ADK）
Models     ← 任意 LLM
```

> 它有两条官方设计原则对理解定位很关键：**Simplicity**（复用既有 HTTP / JSON-RPC / SSE，不重造传输轮子）与 **Opaque Execution**。

## 4.3 单进程**用不到** A2A

不只是"用不到"，而是 **A2A 从设计上就排除这个场景**：

| A2A 的假设 | 本项目的现实 |
|---|---|
| 对方是不透明黑盒 | 在**同一个 Python 对象空间**里，谈不上"不透明" |
| 需要**发现**（Agent Card） | 一个进程内字典就够了 |
| 需要**鉴权**（OAuth2） | 同进程，**天然可信** |
| 需要**长任务状态机** | 有现成的 `TaskManager` + `asyncio` |
| 传输要**跨边界**（HTTPS/SSE） | 内存直接传 |

**A2A 与 MCP 的定位区分**（面试高频）：

| | **MCP** | **A2A** |
|---|---|---|
| 关注点 | **模型 ↔ 数据/外部资源**（工具） | **智能体 ↔ 智能体** |
| 交互对象 | 工具通常**无状态**，执行预定义功能 | 像**智能体/用户**那样通信 |
| 能力范围 | 单次、预定义功能 | 复杂**多轮**交互：推理、规划、委派、**协商**、**请求澄清** |
| 关键提醒 | — | **不要把 agent 包装成简单工具** —— 会阉割它的能力 |

> **一句话记法**：**用 MCP 给 agent 配工具，用 A2A 让 agent 之间协作。**

## 4.4 本项目的协议成熟度自评

**它是一个手写的迷你协议**，缺的正是 3.1 表中"跨进程必须重建"的几样：

| 协议要素 | 有吗 |
|---|:---:|
| 序列化格式 | ✅ JSON |
| 消息 schema | ✅ `MailboxMessage` dataclass |
| 寻址 | ✅ `mailbox/<key>.json` |
| 已读语义 | ✅ `consume()` |
| 传输可靠性（单机） | ⚠️ 文件锁 |
| **发现机制** | ❌ 靠 `AgentNameRegistry`（**进程内单例**） |
| **鉴权** | ❌ 无 |
| **长任务语义** | ❌ 无 Task 状态机 |
| **投递保证** | ❌ 无 ack / 重试 |

---

# 第五部分 · 生命周期：投递状态 vs 记录

## 5.1 ⭐ 拆开"通信完成后该不该删数据"

这里其实是**两个不同问题**共用一个"消息"对象：

| | **投递状态**（driving state） | **记录价值**（record / log） |
|---|---|---|
| 回答什么 | **还要不要重投？** | **事后能查到什么？** |
| 目的 | 决定下一步动作 | 留证据 |
| 完成后 | ✅ **该退休** | ⚠️ **未必该删** |
| 删了会怎样 | 正常 | ❌ **故障现场没了** |

> **"读完就删"是把这两个搅在一起了。** 不想重新注入 → 是**投递状态**问题；不想留数据 → 是**保留策略**问题。**前者不该用后者解决。**

## 5.2 队列 vs 日志

| 模型 | 消费的含义 | 目的 |
|---|---|---|
| **Queue（队列）** | 消费 = **删除消息** | 传递 |
| **Log（日志）** | 消费 = **推进 offset**，消息**永不改** | 传递 **+ 记录** |

> **Kafka 的核心洞察**：**"消费"不是删除消息，而是推进 offset。**
>
> → **投递完成后退休的是 offset（"待投递"这个状态）；数据本身按保留策略留。**

## 5.3 ⭐ 为什么本项目应当"保留"：**已经靠没删救过一次**

`lead.json` 地址不一致那个 bug **之所以能定位，正是因为消息还躺在文件里**，可以 `cat` 出来看到那条未读的 `[idle]`。

> **如果当时是"读完就删"，这个 bug 根本查不出来** —— 连"有没有写过"都无法验证。

而且这也正好说明：

> 点对点的代价是"谁跟谁说了什么，得翻邮箱文件才知道" —— **但"可翻查"恰恰是它的优点。**

**留 vs 删的判断**：

| 留的强理由（agent 场景尤其强） | 删的理由 |
|---|---|
| **可观测性**：多 agent 最贵的能力 | 上下文污染（旧消息被重新注入） |
| **成本几乎为零**（本地 CLI） | 隐私（敏感内容） |
| **可复盘 / 评测** | 长期堆积 |
| 恢复时判断"哪些处理过" | |

**注意右边两条各自对应一个机制，都不需要删数据**：

- "不想重新注入" → **ack 问题**（标记已 ack，不删行）
- "不想留敏感内容" → **TTL / 生命周期问题**（按团队解散时清理）

## 5.4 SQLite 该怎么设计：**用 `acked_at` 代替删除**

```sql
CREATE TABLE messages (
  id           INTEGER PRIMARY KEY,
  team         TEXT NOT NULL,
  from_agent   TEXT NOT NULL,
  to_agent     TEXT NOT NULL,      -- 地址变成一个"列"，不再是文件名
  content      TEXT NOT NULL,
  summary      TEXT,
  message_type TEXT NOT NULL,
  created_at   TEXT NOT NULL,
  acked_at     TEXT,               -- 投递状态：NULL = 待投递
  expires_at   TEXT                -- 生命周期：留给 TTL
);
```

**取出 + 标记一步完成（天然原子）**：

```sql
UPDATE messages SET acked_at = :now
WHERE id IN (
  SELECT id FROM messages WHERE to_agent = :me AND acked_at IS NULL
)
RETURNING *;
```

**→ acked 的行还在表里，只是不再被"待投递"查询命中。** 你要的"不重复注入"和"保留记录"**同时满足**。

**清理按团队生命周期，不逐条删**：团队解散时归档 `team.db` 到 `history/`，或加 TTL。

> **推荐策略**：**投递用 ack，删除按团队生命周期归档。**

## 5.5 同一个道理的既有先例：Checkpoint

`validate_message_chain` 会**截断到最后合法点** —— 敢丢那一轮，是因为**对话上下文是幂等重放的**；而工具结果**不敢重放**（有副作用）。

这与"投递状态可退休、记录要留"是**同一种分层思维**：

| | 驱动状态 | 记录 |
|---|---|---|
| 作用 | 决定"下一步做什么" | 决定"事后能查到什么" |
| 生命周期 | 消费即退休 | 长期保留 |
| 出错时 | 重放 / 重投 | **只能靠它定位** |

---

# 第六部分 · 能力边界：代码强制 vs 提示词驱动

## 6.1 哪部分谁说了算

| 环节 | 谁决定 | 可靠性 |
|------|-------|:---:|
| **能不能发消息**（工具是否下发） | **代码**（`tool_filter.py` 额外注册队友工具） | 🟢 高 |
| **消息怎么投递**（加锁、原子写） | **代码**（`mailbox.py`） | 🟢 高 |
| **谁能发给谁**（寻址、广播） | **代码**（`resolve_recipient`） | 🟢 高 |
| **收到消息**（自动注入上下文） | **代码** | 🟢 高 |
| **干完活自动报 idle** | **代码**（自动，非模型决定） | 🟢 高 |
| **响应 shutdown 退出** | **代码**（`partition_shutdown`） | 🟢 高 |
| **要不要发、发给谁、发什么、何时发** | **模型（提示词）** | 🔴 **低** |

> **结论：基础设施和权限全是代码，只有「通信意图」是提示词驱动的。**

## 6.2 ⚠️ 一个关键不对称

### ✅ 接收是**代码强制**的

```python
# coding_agent/agent.py  主循环内，无条件调用
self._consume_mailbox(conversation)
if self.notification_fn:
    for note in self.notification_fn():
        conversation.add_system_reminder(note)
```

队友**不需要"主动去收信"**，代码每轮自动把新消息塞进上下文。**模型想不收都不行（没有这个开关）。**

### ⚠️ 发送是**模型决定**的

发消息要模型**主动决定调用 `SendMessage`**。这就是不可靠之处 —— 它可能**该说的时候不说**。

```python
# coding_agent/tools/agent_tool.py
TEAMMATE_ADDENDUM = (
    "IMPORTANT: You are running as an agent in a team.\n"
    "Just writing a response in text is not visible to others\n"
    "on your team - you MUST use the SendMessage tool.\n"
)
```

这段提示词在**硬掰**模型别忘了发消息 —— 但它**没有强制力**。

> **所以本项目是"接收有保证、发送无保证"。** 这个不对称**有意为之**（接收不能漏，发送给模型自主权），但也是"仅靠提示词"的风险点。

## 6.3 提升可靠性的四种手段（按强弱）

| # | 手段 | 说明 | 本项目 |
|---|------|------|--------|
| **1** | **结构性约束**（最强） | **不给选择权** —— 想强制星型就直接不下发发送工具 | ✅ **已用**：子 agent 拿不到 `SendMessage`，**天然是纯星型** |
| **2** | **结构化协议** | 用**强类型 tool call** 而非自由文本；参数错误会被拒并促使纠正 | ✅ **已用**：`SendMessageParams` |
| **3** | **显式状态机 / 图** | 节点和边的控制流写成代码（LangGraph 思路） | ❌ 未用 |
| **4** | **协调前移到规划期** | lead 拆任务时就把接口冻结，队友根本不需要对话 | ⚠️ 提示词层面 |

> **原则：能通过"不给能力"实现的约束，永远优于"叫它别做"。**

## 6.4 ⭐ 一句话总原则

> **把"结构"交给代码，把"内容"交给模型。**
>
> - **必须可靠的**（能不能发、发给谁、投递、接收、状态） → **代码**
> - **需要判断的**（说什么、何时说、怎么拆任务） → **提示词**
>
> 而且最佳实践是：**能用"不给能力"实现的约束，就不要用"提示词请求"。**

---

# 第七部分 · 已知缺陷（对照 `multi-agent-issues.md`）

> 状态图例：`✅ 已修` / `❌ 未修` / `⏸ 暂不处理`

| 问题 | 现象 / 根因 | 状态 |
|------|------------|:---:|
| **lead 收件箱地址不一致** | 队友写固定键 `"lead"`，lead 消费用真实 `agent_id` → **两个不同文件**，消息永久滞留；且 `"lead"` 从未注册进名称表 → 按名字发消息必然失败 | ✅ 已修 |
| **名称表是进程级全局** | `AgentNameRegistry` 不按 team 隔离，而队友名唯一性只在团队内检查 → **跨 team 重名静默错投 + 假成功** | ❌ |
| **邮箱锁拿不到时静默继续** | `_with_lock` 在 10 次全因锁冲突失败时，`lock_fd is None` 且 `last_err is None` → **无锁读-改-写**，且 `finally` 会 unlink 别人的锁 | ❌ |
| **pane（tmux/iTerm2）队友未接线** | ① `detect_backend()` 无条件返回 in-process；② 传给子进程的三个环境变量**全仓无人读取**；③ `sub_agent` 建好却**从未 launch**；④ 日志写"falling back"但**实际直接返回错误** | ⏸ |
| **队友收件箱键用了「名字」而非 `agent_id`** | `spawn_inprocess` 读 `<name>.json`，而发送方经名称表解析后写 `<agent_id>.json` → **两个文件**，发给队友的消息**永久滞留**。与 lead 收件箱问题**同源**（都是地址不一致） | ✅ 已修 |
| **`spawn_inprocess` 长驻循环不在生产路径** | 生产路径的队友长驻在 `TaskManager._run_background`；`spawn_inprocess_teammate` 目前只被 benchmark 调用（不传 mailbox → 退化为单次执行），`register_inprocess_handle` 全仓**无调用点** | ⏸ 设计现状 |
| **worktree 隔离子 agent 默认不可达** | 隔离分支读的是 **agent 定义文件里的 `isolation`**（不是工具参数），而四个内置 agent 都没设 | ⏸ |
| **锁用同步 `time.sleep` 做退避** | 调用方是 `async def execute` 且工具标了 `is_concurrency_safe=True` → 竞争时**阻塞整个事件循环**（最长约 1 秒）。但 `_with_lock` 全程无 `await` → 单事件循环内不自我竞争，**当前不可达** | ❌ |
| **任务板重复指派重复通知** | `TaskUpdate` 只判断 `p.assignee` 非空，**不比较是否真的变了** → 模型"改状态时顺手带上同一个 assignee"就会重复通知 | ✅ 已修 |
| **任务板更新侧缺"排除自己"** | 创建侧有 `assignee != self` 的排除，更新侧没有 → **指派给自己也通知自己**（同一语义两处实现的首次漂移） | ✅ 已修 |
| **`assignee` 未归一化，过滤会漏** | `assignee` 原样存自由文本，而 `TaskList(assignee=...)` 是**精确相等**比较 → `alice` / `alice-id-1` / `Alice` 三种写法互相查不到（通知不受影响，但存与查不是同一套） | ❌ |
| **`SharedTaskStore._load()` 静默吞掉文件损坏** | 解析失败直接 `return`，保留旧内存状态 → `get()`/`list_tasks()` 返回**过期数据且不报错**，与"宁可失败也不丢数据"自相矛盾（邮箱 `_read_inbox` 同款） | ❌ |

> **准确说法**：**跨终端队友是未完成的特性，不是可用能力 —— 面试时不要声明支持。**
>
> → 也就是说：**本项目当前的邮箱 100% 在单进程内运行**，跨进程那条路（pane）**没接上线**。

**已修的收口方式**（值得记住的方法论）：

1. **唯一常量** `LEAD_INBOX`（`teams/models.py`）
2. **收件人解析收敛成一份**：`TeamManager.resolve_recipient` + `deliver`
3. **shutdown 语义下沉到 `mailbox`**：`is_shutdown_request` / `partition_shutdown`
4. **空闲标记下沉到生产方**：`TaskManager._run_background` 自己调 `on_teammate_completed`
5. **队友收件箱键统一为 `agent_id`**：`spawn_inprocess` 的注入/轮询改用 `agent.agent_id`，参数改名为 `inbox_key`，并把**不变量写进模块 docstring** + 加回归测试（旧写法下该测试会失败）
6. **任务板指派通知的两条规则收敛到一处**：`should_notify = assignee_changed and assignee and assignee != self`。其中"**是否真的变了**"由 `SharedTaskStore.update()` **在锁内**判定，经 `TaskUpdateResult` 带出 —— 放到锁外"先读再写"会有 **TOCTOU 窗口**

> **贯穿所有修复的同一条原则**：
> **同一语义只能有一个定义处；能靠"位置 / 结构"消除遗漏，就不要靠"每个调用方记得"。**

---

# 第八部分 · 面试讲法（整合版，可直接背）

## 8.1 一句话总纲

> "我的多 agent 通信是**分层设计**的：**拓扑**上默认星型、可选网状；**传输**上用文件邮箱；上面再叠**共享任务板**。而整套设计的判断依据只有一条 —— **边界**。"

## 8.2 分段讲法

**① 先分对象**（最容易被忽略但最关键）：

> "我项目里有**两种形态**的 agent —— 一种是**一次性子 agent**，lead 派任务、阻塞等结果、拿到返回值就结束；另一种是**队友**，长驻、跑在独立 worktree 里、反复接活。**判断标准是生命周期能不能复用，不是对话轮数** —— 子 agent 内部也可能跑几十轮工具调用。
>
> 这里我要纠正一个常见误解：**worktree 隔离和邮箱是两个正交的开关**。我项目里就有'worktree 隔离子 agent'这一档 —— **它有隔离、没有邮箱**，证明**隔离本身根本不需要通信**。需要通信的是'长期共事、并行开工'的队友。而且方向是反直觉的：**隔离越强，通信需求反而越强** —— 正因为队友在各自 worktree 里互相看不见对方的改动，才更需要一条显式通道去对齐接口。**worktree 隔离的是状态，邮箱连的是信息。**"

**② 拓扑**：

> "**拓扑**上我用的是 **Orchestrator-Worker** —— lead 拆任务、派给 teammates、收集结果，这是业界最稳、生产中最多见的模式。但我在上面加了一层**点对点消息**能力，让队友之间可以互相协调，不必事事经过 lead。任务协调则走**共享任务板**，那是 blackboard 模式 —— **状态走任务板（pull）、信号走邮箱（push）**，两套机制缝合起来。
>
> 点对点是**可选能力，不是强制**。全走 lead 的优点是 lead 全知、可追踪、可审计、不会失控；点对点的价值是把 lead 从'**传声筒**'里解放出来 —— 否则每对齐一个细节都要 2 跳，而且 lead 的上下文会被大量与它职责无关的琐事撑爆。**默认从星型开始**，只有当 lead 开始变成瓶颈时才开放点对点。
>
> 不过最关键的一条是：**最可靠的星型不是'禁止通信'，而是'消除通信需求'** —— 让 lead 在拆任务时就把接口冻结，队友根本不需要对话。"

**③ 传输 + 边界**：

> "**传输**上我选了**文件邮箱**而不是消息队列，原因是我的队友设计上可能跑在 tmux/iTerm2 的**独立进程**里，内存队列跨不了进程，而文件邮箱零依赖、跨进程、可持久化、可直接 `cat` 调试。并发写用 `O_CREAT|O_EXCL` 原子锁 + 陈旧锁自动清理保证不丢消息。
>
> 但我想强调一个判断框架 —— **通信的复杂度取决于边界**。进程内你白拿四样东西：**共享内存、共享类型系统、共享生命周期、共享信任**；跨进程这四样全没了，必须显式重建，**这就是'协议'要做的事 —— 本质上协议是在网络上重建你在进程内白拿的那些东西**。所以：同进程用队列/直调，同机跨进程用文件或 SQLite，**跨机才轮到 MQ 或 A2A**。
>
> 我也清楚它的**代价**：轮询有 1 秒延迟，而且**没有投递确认和重试** —— 这是为了本地 CLI 的零依赖做的取舍。"

**④ 契约 vs 协议**（区分度的来源）：

> "这里我要区分两个概念：**契约**是'消息长什么样'，**协议**是'契约 + 寻址 + 传输 + 鉴权 + 生命周期'。**进程内需要契约，不需要协议。** 我项目里其实是**有契约、没有协议** —— `SendMessageParams` 是 pydantic 强类型契约，模型填错会被代码拒绝；但因为不跨边界，就不需要协议那一套。
>
> 而 **A2A 是'语义层'的标准化**，不是传输层 —— 它的设计原则第一条就是复用既有的 HTTP / JSON-RPC / SSE。它保证五件互操作语义：**能力发现（Agent Card）、任务状态机、产物交付、异步长任务、标准鉴权**。它的核心前提是 **Opaque Execution —— 协作双方互不暴露内部逻辑和记忆**，是给**跨组织、跨框架的黑盒 agent** 用的。
>
> **所以单进程根本用不到 A2A**：那个前提在我这里不成立 —— 同进程的两个协程共享同一个对象空间，谈不上'不透明'，也用不到'发现'和'鉴权'。"

**⑤ 生命周期**（最能体现深度的一段）：

> "我把消息的**生命周期分了两层**。'通信完成了'其实牵涉两个东西：**投递状态**和**记录价值**。
>
> **投递状态**确实应该处理完成后退休 —— 我用 `acked_at` 字段实现，**而不是删行**，这样'待投递'查询只命中未 ack 的，不会重复注入。**记录**则不同，它是事后唯一的依据 —— 多 agent 系统最贵的能力就是可观测性，消息一旦读完就删，**故障现场就没了**。
>
> **我自己就踩过这个坑**：之前定位一个队友上报 idle 的收件箱地址对不上的 bug，能查出来正是因为**消息还躺在文件里可以读出来** —— 如果读完就删，这个 bug 根本查不出来。
>
> 这个思路其实就是 **Kafka 的 log 模型**：消费不是删消息，而是推进 offset —— **'销毁'的不是数据，是'待投递'这个状态**。
>
> 顺带我对存储的判断是：**该升级的不是'换成 MQ'，而是'换成 SQLite'**。因为我需要的是**原子性**，不是**投递保证** —— SQLite 零依赖、真事务、崩溃安全，一次解决原子性、并发写和 ack 三个问题。我手写的 10 次重试 + 随机退避 + 陈旧锁清理，**本质上就是在手工重造 SQLite 免费给的事务**。真要跨机、要投递保证，那才是 MQ 的战场。"

**⑥ 能力边界**（架构判断力）：

> "最后一条原则：**把'结构'交给代码，把'内容'交给模型。**
>
> 我项目里**管道的结构和权限全在代码** —— 能不能发（工具是否下发）、发给谁、怎么投递、收到消息自动注入、干完自动报 idle、响应 shutdown，全是代码强制。**只有'要不要发、发什么、何时发'在提示词**。
>
> 这里有个**关键的不对称**：**接收是代码强制的**（主循环每轮自动 consume 并注入上下文，模型想不收都不行），**发送才是模型自主的** —— 所以是'**接收有保证、发送无保证**'，这正是'仅靠提示词不可靠'的根源。
>
> 而最佳实践是：**能通过'不给能力'实现的约束，永远优于'叫它别做'。** 我项目里就用了这招 —— **子 agent 根本拿不到 `SendMessage` 工具，所以天然是纯星型**，比在提示词里叫它别乱发消息可靠一万倍。"

## 8.3 主动暴露的缺口（诚实但加分）

> "我也知道自己的**缺口**在哪：我的文件邮箱其实是个**手写迷你协议** —— 有序列化、schema、寻址、已读语义，但**没有发现机制、没有鉴权、没有投递保证**，那正是跨进程通信必须重建、而我还没重建的部分。
>
> 而且更实际的问题是：**跨进程那条路（pane 队友）目前是未接线的** —— 探测函数无条件返回 in-process，传给子进程的环境变量全仓无人读取。所以准确说，**我的邮箱现在 100% 在单进程内跑**，跨进程能力是设计上预留、实现上没通。
>
> 这不影响架构判断，但**面试时我不会声明支持跨终端队友**。"

---

# 附录 A · 通信相关问答

> 按提问顺序整理，结论优先。

### A1. agent 之间是怎么通信的？

**答：核心是「文件邮箱」** —— 每个 agent 一个 JSON 收件箱文件 + 配套 `.lock` 文件锁。

| 环节 | 实现 |
|---|---|
| 发送 | `SendMessageTool` → `resolve_recipient`（收件人解析）→ `mailbox.write` |
| 并发安全 | `O_CREAT\|O_EXCL` 原子创建锁，重试 10 次，陈旧锁（>10s）自动清理 |
| 接收（队友） | `TaskManager._run_background` 待命循环 `consume(bg.agent.agent_id)` |
| 接收（lead） | `drain_lead_mailbox()` → `consume(LEAD_INBOX)` |
| 注入 | 队友 → `add_user_message`；lead → `add_system_reminder`（XML 包裹） |
| 寻址 | `AgentNameRegistry`（名字→agent_id），支持 `to="*"` 广播 |
| 唤醒面板 | `send_keys_to_pane(pane_id, "")`（进程内队友不需此步） |

**为什么用文件不用消息队列**：队友**设计上**跑在独立进程（tmux/iTerm2），内存队列跨不了进程；文件邮箱零依赖、跨进程、可持久化、可 `cat` 调试。

---

### A2. 邮箱和「子 agent 直接返回结果」有什么区别？为什么要两套？

**答：这是两种不同形态，不是两套实现。**

| | 一次性子 agent | 队友 |
|---|---|---|
| 怎么开 | **不给** `team_name` | **给** `team_name` |
| 生命周期 | 跑完**消失** | **长期在职**，反复接活 |
| 结果怎么拿 | **函数返回值**（`await run_to_completion`） | **邮箱** + 通知队列 |

**为什么子 agent 不用邮箱**：它在**同一个进程**里，调用方阻塞等待，`return` 更简单更快。

**为什么队友不能用返回值**：两个硬约束 ——

1. **不在同一进程**（tmux 是另一个进程）→ 没有"函数返回值"这种东西
2. **长驻 + 无父子关系** → Bob 想给 Alice 发消息时，谁也不是谁的父进程，没法 `await Alice()`；而且 lead 不可能阻塞三小时等它

→ **必须有一个双方都能读写的公共介质**，那就是邮箱。

**一句话**：**子 agent 是"打电话"（问完就挂，结果随电话回来）；队友是"微信群"（一直在线，得有个地方互相留言）。**

---

### A3. 邮箱双方只是两个 agent 吗？开了多个 agent 应该只和 leader 通信吧？

**答：两个都不完全对。**

**① 邮箱不是"两个 agent 之间"，是「每个 agent 一个收件箱」**：

```python
def _inbox_path(self, agent_id: str) -> Path:
    return self._base_dir / f"{agent_id}.json"
```

N 个 agent = **N 个收件箱文件**，且支持两种发法：`to="Alice"`（定向）/ `to="*"`（广播，给所有人各投一份）。

**② "只和 leader 通信"是合法的默认策略，但不是唯一选择** —— 项目**只提供能力，不强制用法**。点对点的价值是把 lead 从"传声筒"里解放出来（否则每对齐一个细节要 2 跳，且 lead 上下文被琐事撑爆）。

**默认建议：从星型开始**；只有当 lead 开始变成瓶颈时才开放点对点。

---

### A4. 这种设计应该是要代码实现的吧，仅靠提示词会有些困难

**答：判断很准 —— 但要分清"哪部分靠代码、哪部分靠提示词"。**

| 环节 | 谁决定 |
|------|-------|
| 能不能发（工具是否下发）、发给谁、怎么投递、收到自动注入、自动报 idle、响应 shutdown | **代码** 🟢 |
| **要不要发、发给谁、发什么、何时发** | **模型（提示词）** 🔴 |

**关键不对称**：**接收是代码强制的**（主循环每轮自动 consume 并注入，模型没有"收信"开关）；**发送才是模型自主的**。→ **接收有保证、发送无保证**。

**提升可靠性的四种手段**（按强弱）：① **结构性约束**（不给能力，最强 —— 本项目已用：子 agent 拿不到 `SendMessage`，天然星型）② **结构化协议**（强类型 tool call）③ **显式状态机/图** ④ **协调前移到规划期**（lead 拆任务时冻结接口）。

> **原则**：**结构给代码，内容给模型；能用"不给能力"实现的约束，永远优于"用提示词请求"。**

---

### A5. 子 agent 到底是「阻塞等」还是「异步并发」？

**答：默认是「真阻塞 + 串行」。"阻塞等返回值"的表述准确，但要加限定。**

三处证据：

1. `AgentTool.is_concurrency_safe = False` → 每个 `Agent` 调用**自成一个串行批次**
2. 主循环 `partition_tool_calls` 只把**可并发**的合并成并行批；执行时仅 `batch.concurrent and len > 1` 才走并行分支 → **模型一口气发 3 个 `Agent` 调用，也是一个接一个跑**
3. `run_to_completion`（子 agent 用的就是它）**更彻底** —— 就是 `for` + `await`，**连批量都没有**

| 模式 | 阻塞？ | 能并发？ | 怎么拿结果 |
|------|:---:|:---:|------|
| **前台子 agent**（默认） | ✅ 真阻塞 | ❌ 串行 | **函数返回值** |
| **后台子 agent**（`run_in_background=True`） | ❌ 立即返回 `task_id` | ✅ 可并发 | `_notify_queue` → `poll_completed()` |
| **fork 模式**（`_enable_fork`） | ❌（**强制**后台） | ✅ 可并发 | 同上 |

> **加这一层，面试官会觉得你真的读过执行路径**，而不是只知道"有个后台参数"。
>
> **顺带一个洞察**：**"`Agent` 工具不可并发"是有意的设计，不是遗漏** —— 子 agent 会改文件、有副作用，并发跑会互相踩（这也正是 worktree 隔离存在的理由）。所以代码在 `is_concurrency_safe=False` 这一处，**用结构强制了"改文件的操作必须串行"** —— 又一次"能用结构约束就不靠提示词"。

---

### A6. 什么叫「进程内用队列，跨进程用协议」？A2A 保证什么？单进程用不到 A2A 吗？

**答：三个问题串起来讲。**

**① 这句话说的是「通信的复杂度取决于边界」**：进程内你**白拿四样东西**（共享内存、共享类型系统、共享生命周期、共享信任），跨进程全没了，必须显式重建 —— **协议就是"在网络上重建你在进程内白拿的那些东西"**。

**② "跨进程用协议"里的"协议"≠ A2A**。协议分三层：**传输层**（HTTP/gRPC/MQ）、**语义层**（A2A / MCP / 自定义 JSON-RPC）、**治理层**（Agent Card / OAuth2）。**A2A 主要是语义层的标准化**，且设计原则第一条就是**复用既有 HTTP/JSON-RPC/SSE，不重造传输轮子**。

**③ A2A 保证五件互操作语义**（**不保证"消息不丢"**）：**能力发现（Agent Card）· 任务状态机 · 产物交付（Artifact）· 异步长任务（SSE + webhook）· 标准鉴权**。核心前提是 **Opaque Execution（不透明执行）** —— 协作双方互不暴露内部逻辑和记忆，**是给跨组织、跨框架的黑盒 agent 用的**。

**④ 单进程用不到 A2A —— 对**。A2A 的每个假设（不透明、需发现、需鉴权、跨边界传输）在同进程里**都不成立**。

**⑤ ⭐ 关键区分：契约 ≠ 协议**

| | **契约** | **协议** |
|---|---|---|
| 是什么 | 消息长什么样（schema） | 契约 + 寻址 + 传输 + 鉴权 + 生命周期 |
| **进程内需要吗** | ✅ **需要** | ❌ **不需要** |

> **本项目有「契约」（`SendMessageParams`），没有「协议」—— 因为没跨边界。**

---

### A7. 消息队列是不是也能实现邮箱的作用（持久化）？

**答：能，MQ 的持久化更强 —— 但要先把「持久化」拆成四层。**

| 层次 | 含义 | 邮箱 | MQ |
|---|---|:---:|:---:|
| ① 字节落盘 | 重启后消息还在 | ✅ | ✅ |
| ② 消费状态落盘 | 重启后知道哪些读过 | ✅ `read` 字段 | ✅ offset/ack |
| ③ **地址稳定** | 重启后还找得到自己的收件箱 | ⚠️ | ✅ |
| ④ **恢复语义** | 重启后未处理的会重新投递 | ❌ | ✅ |

**本项目只做到 ①②**：

- **③ 不稳**：`self.agent_id = uuid.uuid4().hex[:12]`（每次进程启动都是新 id），收件箱文件名就是 `<agent_id>.json` → **teammate 重启后旧消息变孤儿**（lead 例外：读的是 `config.json` 里持久化的 `LEAD_INBOX`）
- **④ 缺失**：**没有任何重启路径会去读它** → 所以是"**落盘但不可恢复**"
- **崩溃即丢**：`consume()` 先标记已读再返回 → **at-most-once**

**MQ 能多给**：ACK/重投、消费组、真 pub/sub（对比：本项目 broadcast 是**写 N 份**）、保留回放、背压、lag 可观测。

**但代价是要跑一个 broker** —— 单机 CLI、消息量极小，**引入 Kafka 是拿运维复杂度换不需要的能力**。

**⭐ 关键判断：需要的不是"投递保证"，而是"原子性"** → 所以答案不是 MQ，是**SQLite**。

---

### A8. agent 通信用 SQLite 实现？通信完成后数据应该销毁吧？需要保留做回放吗？

**答：这里有个陷阱 —— "通信完成后"牵涉两个生命周期。**

| | **投递状态**（driving state） | **记录价值**（record） |
|---|---|---|
| 回答什么 | 还要不要重投？ | 事后能查到什么？ |
| 完成后 | ✅ **该退休** | ⚠️ **未必该删** |
| 删了会怎样 | 正常 | ❌ **故障现场没了** |

**"读完就删"是把两个问题搅在一起了。** 不想重新注入 → 是**投递状态**问题；不想留数据 → 是**保留策略**问题。**前者不该用后者解决。**

**这正是「队列」vs「日志」的分野** —— **Kafka 的核心洞察：消费不是删消息，而是推进 offset。** → **"销毁"的不是数据，是"待投递"这个状态。**

**★ 最有力的论证：本项目已经靠"没删"救过一次** —— `lead.json` 那个 bug 之所以能定位，正是因为消息还躺在文件里可以 `cat` 出来。**如果读完就删，这个 bug 根本查不出来。**

**SQLite 该这么设计**：

- **用 `acked_at` 代替删除** —— acked 的行还在表里，只是不再被"待投递"查询命中 → "不重复注入"和"保留记录"**同时满足**
- **清理按团队生命周期** —— 团队解散时归档 `team.db`，不逐条删

> **推荐策略：投递用 ack，删除按团队生命周期归档。** 对本地 CLI，**"归档不删"是免费午餐**。

**⚠️ 边界**：**SQLite 的前提是"同机"**（NFS 上锁不可靠）；**跨机器才轮到 MQ / A2A**。

---

### A9. SQLite 就是"每次都去查数据库"吗？

**答：基本对，但要校准三处。**

**① SQLite 不是"另一个邮箱"，是邮箱的「实现载体」**：

| 层 | 现在 | 换 SQLite 后 |
|---|---|---|
| **接口**（`write`/`consume`/`broadcast`） | — | ✅ **完全不变** |
| **存储** | JSON + 手写锁 | 一张表 + 事务 |
| **语义** | at-most-once | ⬆️ 可升 at-least-once |

→ **对上层调用方零改动**（接口不变、实现替换）。

**② 分清「谁在查」，且发/收是两种操作**：

| 动作 | SQL 操作 | 谁触发 |
|---|---|---|
| **发消息** | **INSERT**（写一行） | **模型**决定调 `SendMessage` |
| **收消息** | **SELECT + UPDATE**（原子取出并 ack） | **代码**在主循环每轮自动做 |

> ⭐ **模型自己不碰数据库** —— 它只看到 `SendMessage` 工具和注入的消息文本。**发是写、收是查，而且都不是模型在查。**

**③ 轮询还在（SQLite 没有推送），但成本降一个数量级**：

| | JSON 文件 | SQLite |
|---|---|---|
| 每轮成本 | 读整个文件 + 反序列化 + **写回整个文件** | 索引查询，**没命中几乎零成本** |
| 复杂度 | O(**所有**消息) | O(**命中**消息) |

> 技巧：`PRAGMA data_version` —— 廉价检测"库是否被改过"，没改就完全跳过查询。

**④ 顺带的升级：广播从"写 N 份"变"写 1 次"** —— 规范成 `messages`（消息体）+ `deliveries`（每收件人一行）两张表。"**已读"本来就是"每收件人一份"**，正好对应。**这就是 MQ 的标准做法**，也说明设计在向 log 模型收敛。

---

### A10. 既然 worktree 隔离了，为什么还要邮箱让 agent 之间通信？

**答：因为把两个正交维度绑在一起了。**

**证据**：项目里有"**worktree 隔离子 agent**"这一档 —— **它有隔离、没有邮箱** → 证明 **隔离本身根本不需要通信**。

| | 解决什么 | 性质 |
|---|---|---|
| **worktree** | 文件冲突、工作区污染 | **物理**隔离（状态） |
| **邮箱** | 信息同步 | **逻辑**连接（信息） |

**队友为什么需要点对点**（真实痛点）：① **接口对齐**（A 改签名要通知 B）② **共享发现**（某 API 已废弃，B 也在用）③ **依赖协调**（B 等 A 先产出接口）④ **冲突声明**（"这块我负责"）。

**⭐ 反直觉但重要**：**隔离越强，通信需求越强** —— 正因为队友互相看不见对方的改动（这正是隔离的目的），才更需要一条**显式通道**同步关键信息。

**同时要诚实**：**点对点是"可选增强"，不是架构必需** —— 纯 orchestrator-worker 全走 lead 也能跑（本项目第 2 档就是），只是 lead 会成为瓶颈。

---

# 附录 B · 相邻主题问答

> 这三块不属于"通信"，但同期问过，一并存下。

### B1. 三种唤醒的分派条件（`AgentTool.execute`）

**答：一个「短路式级联」，顺序即优先级。**

| 分支 | 触发条件 | 执行方式 |
|------|---------|---------|
| **队友**（最高优先） | `params.team_name` **非空** | `_execute_as_teammate`，内部先建 worktree，再按后端分派 |
| **worktree 隔离子 agent** | 非队友，且定义里 `isolation == "worktree"` | 前台阻塞，跑完 `auto_cleanup` |
| **后台** | 非队友、非 worktree，且 `run_in_background=True` **或** `definition.background=True` | `task_manager.launch(...)` 立即返回 `task_id` |
| **前台**（默认） | 上面的都不满足 | `await sub_agent.run_to_completion(p.prompt)` 阻塞到结果 |

**三个容易考的点**：

1. **`team_name` 判断在最前面** —— 有值就短路，**连 worktree 判断都跳过**（队友内部自己建）
2. **`_enable_fork=True` 强制所有子 agent 走后台** —— 否则前台阻塞会卡死主循环
3. **队友分支内部再分后端**：tmux/iTerm2 → `_spawn_pane_teammate`（独立进程，只能靠文件邮箱）；其他 → `task_manager.launch`（进程内 asyncio）

**前台 vs 后台的语义差异**：前台返回**结果文本**（并顺手把子 agent 结论沉淀进主 agent 记忆库）；后台返回**任务 ID + 提示语**（"Do NOT wait, sleep, or poll"），结果走 `_notify_queue` 异步通知。

---

### B2. Checkpoint：save 触发点 / 恢复入口 / `validate_message_chain`

**① save 触发点** —— 在 `run_to_completion` 主循环内，**每轮工具结果写入对话之后**：

```python
# coding_agent/agent.py
conversation.add_tool_results_message(tool_results)

# 任务级 Checkpoint：每轮工具执行完成后落盘，支持断点续跑
try:
    self.checkpoint.save(conversation)
except Exception:
    log.debug("Failed to save checkpoint", exc_info=True)
```

**关键**：在 `turn_end` hook **之前**，且包在 `try/except` 里 —— **落盘失败绝不影响主流程**。

**save 内部两道门槛**：① 没配 `session_dir` → 跳过；② **没有任何工具调用产物 → 不落盘**（避免纯聊天轮次写空 checkpoint）。

**② 恢复入口** —— `run_to_completion(..., resume_from=...)`：**只有 `conversation is None`（全新任务）且传了 `resume_from`** 才尝试恢复；恢复出的对话为空则视为没恢复成功。

**③ `validate_message_chain` 校验什么** —— 校验 **`tool_use` 与 `tool_result` 的配对完整性**，**返回一个下标**（"最后一个所有 `tool_use` 都已配对完毕的位置"），恢复时截断 `records[:valid]`。

**为什么必须做**：checkpoint 虽是每轮落盘，但若进程死在"assistant 发出 `tool_use`"和"`tool_result` 写入"之间，文件尾部会留一条**孤儿 `tool_use`**。直接重建对话发给 LLM，Anthropic 会因 `tool_use` 没有对应 `tool_result` 直接 **400**。所以**截断到最后一个协议合法的前缀**。

**代价（要诚实讲）**：会丢掉那个未完成的工具轮次，恢复后需**重做那一轮**。这是刻意取舍 —— **重放对话上下文是幂等的，而重放工具可能产生副作用**。

---

### B3. MCP 三层 + 延迟加载

**① `client.py` — 连接层（管"一条管道"）**：只负责到**某一个** MCP 服务器的连接收发，不理解工具语义。stdio（`StdioServerParameters` + `build_child_env`，stderr 丢 `devnull`）/ HTTP（`streamable_http_client` + `httpx`，headers 支持 `${ENV}` 解析）。用 `AsyncExitStack` 统一管生命周期；`_alive` 标记存活；暴露 `list_tools()` / `call_tool()` / `instructions`。

**② `tool_wrapper.py` — 适配层（把 MCP tool 变成本地 Tool）**：

- 名字规范化为 `mcp_{server}_{tool}`（避免重名，且**名字自解释**）
- **JSON Schema → pydantic 动态建模**（`create_model`）→ 能**复用本地统一的参数校验链路**
- `get_schema()` **透传原始 `inputSchema`** 给 LLM
- **`should_defer = True`**（延迟，省 token 的关键）
- `is_concurrency_safe = False`（远端工具默认不并行）
- `execute()` 带**断线重连**：`is_alive` 为假先 `connect()`；异常则置 `_alive=False` 并返回 error（下次自动重连）

**③ `manager.py` — 编排层（管"多个服务器"）**：`connect_all` **逐服务器**建 client、连接、收 instructions 封成 `ServerInfo`、`list_tools` 后逐个包 wrapper；**单服务器失败不影响整体**（`result.errors`）；`register_all_tools` 注册进 `ToolRegistry`；`get_client` 支持按需连接 + 断线重建；`shutdown` 统一关闭。

**延迟加载完整流程**（`manager` + `registry` + `ToolSearch` + `agent.py` 四方协作）：

| Step | 做什么 |
|:---:|------|
| 1 | **全量注册**：所有 MCP 工具都进 registry，但都带 `should_defer=True` |
| 2 | **schema 不发**（省 token 根源）：`get_all_schemas` 里 `if should_defer and name not in _discovered: continue` |
| 3 | **但把"目录"告诉模型**：`get_deferred_tool_names()` 拼成 system-reminder 注入 |
| 4 | **模型主动加载**：`ToolSearch.execute` —— `select:<name>` 精确取 / 否则**关键词打分检索** |
| 5 | **标记已发现**：`mark_discovered(name)` |
| 6 | **下一轮注入**：完整 schema 进请求，且从 reminder 名单中消失 |
| 7 | **兜底**：检索未命中 → 返回错误 + **附上全部可用延迟工具名**，给二次机会 |

**两个诚实点**：① `search_deferred` 是**关键词打分**，不是向量语义检索（兜底返回全名单在弥补这点）；② `_discovered` 是 registry 上的**会话级 set**，**一旦发现就常驻** —— 省 token 是"首次加载前省"，不是每轮都省。

---

### B4. 延迟加载 = "只加载工具名字"吗？

**答：基本对，但有两处要修正。**

| 你的理解 | 对不对 |
|---------|-------|
| 接了 MCP server | ✅ 真连接（`connect()` + `list_tools()` 全跑） |
| 不全量加载里面的工具 | ⚠️ **只有一半对** —— 工具在本地**已全量注册**，延迟的只是"**注入给 LLM**" |
| 只加载工具名字 | ✅ |
| **和简短描述** | ❌ **描述也不发，只发名字** |

**修正 1：延迟的不是"加载"，是"注入"**。连接、枚举、注册**全量做完**，wrapper（含完整 `inputSchema` + `description`）都在 registry 里；真正延迟的是"**要不要把它放进发给 LLM 的请求**"。

> **准确说法**：不是"懒加载工具"，而是"**懒注入 schema**"。省的不是和 MCP server 的通信，而是**发往 LLM 的 prompt 体积**。

**修正 2：给模型的只有名字，没有描述**。`get_deferred_tool_names()` 返回 **`list[str]`**，拼进去的就是纯名字列表（如 `mcp_postgres_run_query`）。

**描述在哪？** 在本地 registry 里，**只用于 `search_deferred` 的关键词打分**，**从不进上下文**。那点"语义线索"其实来自**命名规范本身**（`mcp_{server}_{tool}` 是自解释的）。

**直觉类比**：把 MCP 工具想成**一柜子说明书** —— 全量连接 = 柜子搬进屋里、说明书全部归档；延迟加载 = 不把整柜塞进对话，**只贴一张目录在墙上**；ToolSearch = 模型说"我要 X"，才**只抽 X 那本**；`mark_discovered` = 抽过的就常驻。

**可能被追问：只给名字，模型怎么知道该不该用？** → 这确实是有意的取舍，有两层补偿：**① 名字自解释** ② **`ToolSearch` 兜底**（未命中就返回全部工具名）。代价是"模型可能不知道某工具存在"，需要系统提示词里那句"**没有你需要的工具就用 ToolSearch 找**"兜住。

---

### B5. `lead.json` bug 的定位与验证方法（**方法论**）

**问题**：队友上报 idle 写的是硬编码 `"lead"` → `lead.json`；而 lead 消费用的是真实 `agent_id` → `<uuid>.json` —— **两个不同文件**，消息**永久滞留**。且 `"lead"` 从未注册进名称表 → `SendMessage(to="lead")` **必然解析失败**。

**最讽刺的是**：`TeamCreateTool` 的描述**明确教**队友这么干 → 说明是**实现漏了，不是设计如此**。

**验证方法（四步，值得复用）**：

> **① 找出同一语义的所有实现处 → ② 确认真实代码里哪条路是"活的" → ③ 最小复现 → ④ 反证**

**为什么第 ④ 步是灵魂**：多数人只会说"我怀疑有 bug"；**能反证的才叫定位** —— 即"把可疑点改对之后问题立刻消失"，才能排除"机制本身也坏了"。

**实际操作**：写最小脚本验证 ① 文件名不同 ② `resolve("lead") = None` ③ 写入后 lead 消费 0 条、消息滞留 ④ **反证**：按真实 `agent_id` 写入后立刻能消费到 → **证明投递机制本身没问题，纯是地址拼错**。

**最终修法**：**统一到唯一常量 `LEAD_INBOX`**（因为**邮箱目录本就按 team 隔离**，`"lead"` 键在团队内唯一）+ **收件人解析收敛成一份**（`resolve_recipient` + `deliver`）。

**面试话术**：

> "我在梳理多 agent 通信时，发现**队友上报 idle 的收件箱地址和 lead 实际消费的地址对不上** —— 队友写硬编码的 `'lead'`，但 lead 读真实 `agent_id`，**两个文件**。而且 `'lead'` 从没注册进名称解析表，所以 `SendMessage(to='lead')` 会直接失败。而工具自己的描述还明确教队友这么做，说明是实现漏了。
>
> 我不只是静态看代码 —— 我**写了个最小复现**验证写入后 lead 消费为 0 条、消息永久滞留；然后**做了反证**，把地址换成真实 `agent_id` 后立刻能消费到。这确认了投递机制本身没问题，**纯粹是地址拼接的错误**。
>
> 修的时候我没有加别名，而是**统一到唯一常量** —— 因为邮箱目录本来就按 team 隔离，这个键在团队内天然唯一；同时把收件人解析收敛成一份，杜绝两套地址逻辑再次漂移。"

---

## 附 · 一句话索引

| 结论 | 出处 |
|------|------|
| **隔离的是状态，通信的是信息；两者正交** | 第一部分 |
| **隔离越强，通信需求越强** | 第一部分 |
| **协议 = 在网络上重建你在进程内白拿的那些东西** | 第三部分 |
| **进程内需要契约，不需要协议** | 第四部分 |
| **A2A 给黑盒间协作用；单进程用不到** | 第四部分 |
| **MQ 给跨机高吞吐用；单机 CLI 用 SQLite** | 第三部分 |
| **需要的不是投递保证，是原子性** | 第三部分 |
| **"销毁"的不是数据，是"待投递"这个状态** | 第五部分 |
| **结构给代码，内容给模型** | 第六部分 |
| **能用"不给能力"实现的约束，优于"用提示词请求"** | 第六部分 |
| **接收有保证、发送无保证** | 第六部分 |
| **最可靠的星型是消除通信需求** | 第二部分 |
| **声明了 ≠ 接线了** | 第七部分 |
| **持久 ≠ 可达** | 第七部分 |
